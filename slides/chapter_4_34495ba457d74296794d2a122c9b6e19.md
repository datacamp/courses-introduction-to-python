---
title: Insert title here
key: 34495ba457d74296794d2a122c9b6e19
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/vi-VN/038d6564-b8ca-4f07-a0dd-33963f203346.mp3
---

## NumPy: Thống kê cơ bản

```yaml
type: TitleSlide
key: 5d21c4b49f
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
Bước đầu tiên rất phổ biến khi phân tích dữ liệu
---

## Phân tích dữ liệu

```yaml
type: FullSlide
key: 32899f8a31
```

`@part1`
- Hiểu rõ dữ liệu của bạn{{1}}

- Dữ liệu ít ỏi -> nhìn là hiểu{{2}}

- Dữ liệu lớn -> ?{{3}}

`@script`
đó là làm quen với dữ liệu trước đã. Với các NumPy ở phần trước, việc này khá đơn giản vì dữ liệu không nhiều. Tuy nhiên, khi làm khoa học dữ liệu, ta sẽ phải xử lý hàng nghìn, thậm chí hàng triệu hay hàng tỷ con số.
---

## Khảo sát toàn thành phố

```yaml
type: FullSlide
key: df02059657
```

`@part1`
```py
import numpy as np
np_city = ... # Implementation left out
np_city
```{{1}}

```out
array([[1.64, 71.78],
       [1.37, 63.35],
       [1.6 , 55.09],
       ...,
       [2.04, 74.85],
       [2.04, 68.72],
       [2.01, 73.57]])
```{{1}}

`@script`
Hãy tưởng tượng chúng ta phải thực hiện một khảo sát trên toàn thành phố, hỏi năm nghìn người trưởng thành về chiều cao và cân nặng của họ. Cuối cùng bạn thu được một thứ như thế này: một NumPy array hai chiều, tạm đặt tên là np_city, có năm nghìn hàng, tương ứng với năm nghìn người, và hai cột, tương ứng với chiều cao và cân nặng.
Chỉ ngồi nhìn chằm chằm vào những con số này thì sẽ không hiểu gì cả. Thay vào đó, chúng ta có thể tạo các thống kê tổng quan để hiểu dữ liệu tốt hơn.
---

## NumPy

```yaml
type: FullSlide
key: d3c991b91f
code_zoom: 90
```

`@part1`
```py
np.mean(np_city[:, 0])
```{{1}}

```out
1.7472
```{{1}}

```py
np.median(np_city[:, 0])
```{{2}}

```out
1.75
```{{2}}

`@script`
Ngoài việc là một cấu trúc dữ liệu hiệu quả để làm các phép tính số học, NumPy còn rất mạnh trong việc tạo các thống kê kiểu này. 

Trước hết, ta thử tìm chiều cao trung bình của 5.000 người với hàm mean của NumPy. Vì đây là hàm từ package NumPy, nhớ phải bắt đầu bằng np..

Tất nhiên, trước đó, bạn phải trích xuất để lấy cột chiều cao từ array hai chiều. Có vẻ trung bình mọi người cao 1,75 mét. Thế còn số trung vị là bao nhiêu? Trung vị là chiều cao của người đứng giữa nếu ta sắp xếp tất cả từ thấp đến cao. Thay vì viết code Python phức tạp, ta chỉ cần dùng hàm median của NumPy:

Chúng ta cũng có thể làm tương tự cho cột cân nặng trong np_city. Thường thì các thống kê tóm tắt này sẽ giúp ta kiểm tra tính hợp lý của dữ liệu. Nếu kết quả ra cân nặng trung bình 2.000 ki-lô-gam, thì gần như chắc chắn dữ liệu của ta bị sai. 


Ngoài mean và median, còn có các hàm khác,
---

## NumPy

```yaml
type: FullSlide
key: a66131c711
```

`@part1`
```py
np.corrcoef(np_city[:, 0], np_city[:, 1])
```

```out
array([[ 1.     , -0.01802],
       [-0.01803,  1.     ]])
```

```py
np.std(np_city[:, 0])
```{{1}}

```out
0.1992
```{{1}}

- sum(), sort(), ...{{2}}

- Chỉ một kiểu dữ liệu: nhanh hơn!{{3}}

`@script`
như corrcoeff để kiểm tra xem chiều cao và cân nặng có tương quan không,

hay std để tính độ lệch chuẩn.

NumPy cũng có các hàm cơ bản như sum và sort, vốn đã có trong bản Python tiêu chuẩn. Tuy nhiên, khác biệt lớn ở đây là tốc độ. Vì NumPy chỉ cho phép một kiểu dữ liệu duy nhất trong mỗi array nên nó có thể tăng tốc độ tính toán lên nhiều lần.
---

## Tạo dữ liệu

```yaml
type: FullSlide
key: 0c27803967
code_zoom: 80
```

`@part1`
- Đối số (argument) dùng cho `np.random.normal()` {{1}}
	- Giá trị trung bình của phân phối dữ liệu{{1}}
    - Độ lệch chuẩn của phân phối dữ liệu{{1}}
    - Số lượng mẫu{{1}}

```py
height = np.round(np.random.normal(1.75, 0.20, 5000), 2)

weight = np.round(np.random.normal(60.32, 15, 5000), 2)

```{{1}}
```py
np_city = np.column_stack((height, weight))
```{{2}}

`@script`
Một lưu ý nhỏ: Nếu bạn thắc mắc dữ liệu trong video này được tạo ra thế nào: bọn mình đã mô phỏng bằng chính các hàm NumPy! Mình đã lấy mẫu ngẫu nhiên 5000 lần từ hai tập hợp dữ liệu để tạo mảng chiều cao và cân nặng, rồi dùng column_stack để ghép chúng thành hai cột. Đây là một chức năng rất hay khác của Numpy.

Còn một công cụ tuyệt vời nữa để hiểu dữ liệu, đó là trực quan hóa, nhưng nội dung đó sẽ được đề cập trong khóa học tiếp theo.
---

## Cùng thực hành nào!

```yaml
type: FinalSlide
key: c4df18cfc1
```

`@script`
Giờ thì hãy chuyển sang phần bài tập để khám phá các NumPy array của bạn nào!
