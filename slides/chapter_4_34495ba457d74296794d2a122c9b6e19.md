---
title: Insert title here
key: 34495ba457d74296794d2a122c9b6e19
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/fr-CA/d35e0e1e-87ac-47c5-b84d-a3a777989468-37e2dbc5190095869b6e3d2fd2d280c8.mp3
---

## NumPy : statistiques de base

```yaml
type: TitleSlide
key: 5d21c4b49f
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
Une première étape typique pour analyser vos données,

---

## Analyse de données

```yaml
type: FullSlide
key: 32899f8a31
```

`@part1`
- Faites connaissance avec vos données{{1}}

- Peu de données → regardez-les simplement{{2}}

- Beaucoup de données → ?{{3}}

`@script`
c'est d'abord d'apprendre à bien connaître vos données. Pour les tableaux NumPy d'avant, c'est plutôt simple, parce qu'il n'y a pas beaucoup de données. Par contre, en tant que scientifique des données, vous allez traiter des milliers, voire des millions ou des milliards de nombres.

---

## Sondage à l’échelle de la ville

```yaml
type: FullSlide
key: df02059657
```

`@part1`
```py
import numpy as np
np_city = ... # Implementation left out
np_city
```{{1}}

```out
array([[1.64, 71.78],
       [1.37, 63.35],
       [1.6 , 55.09],
       ...,
       [2.04, 74.85],
       [2.04, 68.72],
       [2.01, 73.57]])
```{{1}}

`@script`
Imaginez que vous menez un sondage à l'échelle de la ville où vous demandez à cinq-mille adultes leur taille et leur poids. Vous obtenez quelque chose comme ceci : un tableau NumPy deux-D, que j'ai nommé np_city, avec cinq-mille lignes, correspondant aux cinq-mille personnes, et deux colonnes, correspondant à la taille et au poids.

Fixer ces nombres comme un zombie ne vous apprendra rien. Par contre, vous pouvez générer des statistiques sommaires sur vos données.

---

## NumPy

```yaml
type: FullSlide
key: d3c991b91f
code_zoom: 90
```

`@part1`
```py
np.mean(np_city[:, 0])
```{{1}}

```out
1.7472
```{{1}}

```py
np.median(np_city[:, 0])
```{{2}}

```out
1.75
```{{2}}

`@script`
En plus d'offrir une structure de données efficace pour le calcul numérique, NumPy est justement très bon pour ce genre de choses.

Pour commencer, vous pouvez chercher la taille moyenne de ces cinq-mille personnes avec la fonction mean de NumPy. Comme c'est une fonction du module NumPy, n'oubliez pas de commencer par np.. 

Bien sûr, j'ai d'abord dû sélectionner la colonne de la taille à partir du tableau deux-D. Il semble qu'en moyenne, les gens mesurent un-virgule-soixante-quinze mètre. Et la médiane de taille? C'est la taille de la personne du milieu si vous classez tout le monde de la plus petite à la plus grande. Plutôt que d'écrire du code Python compliqué pour trouver cela, vous pouvez simplement utiliser la fonction median de NumPy :

Vous pouvez faire des opérations similaires pour la colonne du poids dans np_city. Souvent, ces statistiques sommaires vous offrent une « vérification de cohérence » de vos données. Si vous aboutissez à un poids moyen de deux-mille kilogrammes, vos mesures sont fort probablement erronées.

Outre la moyenne et la médiane, il existe aussi d'autres fonctions,

---

## NumPy

```yaml
type: FullSlide
key: a66131c711
```

`@part1`
```py
np.corrcoef(np_city[:, 0], np_city[:, 1])
```

```out
array([[ 1.     , -0.01802],
       [-0.01803,  1.     ]])
```

```py
np.std(np_city[:, 0])
```{{1}}

```out
0.1992
```{{1}}

- sum(), sort(), ...{{2}}

- Type unique imposé : performance !{{3}}

`@script`
comme corrcoeff pour vérifier, par exemple, si la taille et le poids sont corrélés,

et std, pour l'écart type.

NumPy comprend aussi des fonctions plus de base, comme sum et sort, qui existent également dans la distribution de base de Python. Cependant, la grande différence ici, c'est la vitesse. Parce que NumPy impose un type de données unique dans un tableau, il peut accélérer les calculs de façon marquée.

---

## Générer des données

```yaml
type: FullSlide
key: 0c27803967
code_zoom: 80
```

`@part1`
- Arguments pour `np.random.normal()` {{1}}
	- moyenne de la distribution{{1}}
    - écart-type de la distribution{{1}}
    - nombre d’échantillons{{1}}

```py
height = np.round(np.random.normal(1.75, 0.20, 5000), 2)

weight = np.round(np.random.normal(60.32, 15, 5000), 2)

```{{1}}
```py
np_city = np.column_stack((height, weight))
```{{2}}

`@script`
Petite parenthèse : si vous vous demandez d'où viennent les données de cette vidéo, nous les avons simulées avec des fonctions NumPy! J'ai échantillonné deux distributions aléatoires cinq-mille fois pour créer les tableaux de taille et de poids, puis utilisé column_stack pour les coller comme deux colonnes. Une autre chose formidable que NumPy sait faire!

Un autre excellent outil pour se faire une idée de vos données, c'est de les visualiser, mais ça, ce sera pour le prochain cours aussi.

---

## Passons à la pratique !

```yaml
type: FinalSlide
key: c4df18cfc1
```

`@script`
Commencez par passer aux exercices pour apprendre à explorer vos tableaux NumPy!
