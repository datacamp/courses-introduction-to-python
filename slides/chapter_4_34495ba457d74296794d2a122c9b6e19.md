---
title: Insert title here
key: 34495ba457d74296794d2a122c9b6e19
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/es-ES/3db6d812-d464-4417-a721-3fce6e10ee09.mp3
---

## NumPy: estadística básica

```yaml
type: TitleSlide
key: 5d21c4b49f
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
Un primer paso típico en el análisis de datos

---

## Análisis de datos

```yaml
type: FullSlide
key: 32899f8a31
```

`@part1`
- Conoce tus datos{{1}}

- Pocos datos -> míralos{{2}}

- Macrodatos .> ?{{3}}

`@script`
es, en primer lugar, conocer tus datos. Para las matrices NumPy anteriores, esto es bastante fácil, porque no hay muchos datos. Sin embargo, como científico de datos, tendrás que procesar miles, si no millones o miles de millones, de números.


---

## Encuesta a toda la ciudad

```yaml
type: FullSlide
key: df02059657
```

`@part1`
```py
import numpy as np
np_city = ... # Implementation left out
np_city
```{{1}}

```out
array([[1.64, 71.78],
       [1.37, 63.35],
       [1.6 , 55.09],
       ...,
       [2.04, 74.85],
       [2.04, 68.72],
       [2.01, 73.57]])
```{{1}}

`@script`
Imagina que realizas una encuesta en toda la ciudad donde preguntas a 5000 adultos su altura y peso. El resultado es algo así: una matriz numpy 2D, al que he llamado np_city, que tiene 5000 filas correspondientes a las 5000 personas y 2 columnas correspondientes a la altura y el peso.

Limitarte a mirar estos números como un zombi no te aportará ninguna idea. Sin embargo, lo que sí puedes hacer es generar estadísticas resumidas sobre tus datos.


---

## NumPy

```yaml
type: FullSlide
key: d3c991b91f
code_zoom: 90
```

`@part1`
```py
np.mean(np_city[:, 0])
```{{1}}

```out
1.7472
```{{1}}

```py
np.median(np_city[:, 0])
```{{2}}

```out
1.75
```{{2}}

`@script`
Además de contar con una estructura de datos eficiente para el procesamiento de números, NumPy también es muy útil para este tipo de tareas.

Para empezar, puedes intentar averiguar la altura media de estas 5000 personas con la función mean de NumPy. Como se trata de una función del paquete NumPy, no olvides comenzar con np.

Por supuesto, primero tuve que realizar una operación de subconjunto para obtener la columna de altura de la matriz 2D. Parece que, de media, las personas miden 1,75 metros. ¿Y la altura mediana? Es la altura de la persona del medio si ordenas a todas las personas de menor a mayor. En lugar de escribir un código Python complicado para averiguarlo, puedes utilizar la función median de NumPy:

Puedes hacer lo mismo con la columna weight en np_city. A menudo, estas estadísticas resumidas te proporcionarán una comprobación de coherencia de tus datos. Si obtienes un peso medio de 2000 kilogramos, es muy probable que las medidas sean incorrectas.

Además de la media y la mediana, también hay otras funciones,


---

## NumPy

```yaml
type: FullSlide
key: a66131c711
```

`@part1`
```py
np.corrcoef(np_city[:, 0], np_city[:, 1])
```

```out
array([[ 1.     , -0.01802],
       [-0.01803,  1.     ]])
```

```py
np.std(np_city[:, 0])
```{{1}}

```out
0.1992
```{{1}}

- sum(), sort(), ...{{2}}

- Aplica un único tipo de datos: velocidad{{3}}

`@script`
como corrcoeff, para comprobar si, por ejemplo, la altura y el peso están correlacionados,

o std, para la desviación estándar.

NumPy también incluye funciones más básicas, como sum y sort, que también existen en la distribución básica de Python. Sin embargo, la gran diferencia aquí es la velocidad. Dado que NumPy impone un único tipo de datos en una matriz, puede acelerar considerablemente los cálculos.


---

## Generar datos

```yaml
type: FullSlide
key: 0c27803967
code_zoom: 80
```

`@part1`
- Argumentos para `np.random.normal()` {{1}}
	- media de distribución{{1}}
    - desviación estándar de distribución{{1}}
    - número de muestras{{1}}

```py
height = np.round(np.random.normal(1.75, 0.20, 5000), 2)

weight = np.round(np.random.normal(60.32, 15, 5000), 2)

```{{1}}
```py
np_city = np.column_stack((height, weight))
```{{2}}

`@script`
Solo una nota al margen: si te preguntas cómo he obtenido los datos que aparecen en este vídeo, los hemos simulado con funciones NumPy. Tomé dos distribuciones aleatorias 5000 veces para crear las matrices de altura y peso, y luego utilicé column_stack para pegarlos juntos como dos columnas. ¡Otra cosa increíble que puede hacer NumPy!

Otra herramienta excelente para comprender mejor tus datos es visualizarlos, pero eso lo veremos en el próximo curso.


---

## ¡Vamos a practicar!

```yaml
type: FinalSlide
key: c4df18cfc1
```

`@script`
Primero, dirígete a los ejercicios para aprender a explorar matrices de NumPy.
