---
title: Insert title here
key: 34495ba457d74296794d2a122c9b6e19
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/fr-FR/ef575688-a67a-4dfb-a439-4a35ed6c48c2.mp3
---

## NumPy : Statistiques de base

```yaml
type: TitleSlide
key: 5d21c4b49f
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
Une première étape classique dans l'analyse de vos données consiste à

---

## Analyse des données

```yaml
type: FullSlide
key: 32899f8a31
```

`@part1`
- Apprenez à connaître vos données{{1}}

- Peu de données - il suffit de les examiner{{2}}

- Big data > ? {{3}}

`@script`
bien connaître vos données. Pour les tableaux NumPy précédents, cela est relativement simple, car il y a peu de données. Cependant, en tant que data scientist, vous serez amené à traiter des milliers, voire des millions ou des milliards de chiffres.


---

## Enquête à l'échelle de la ville

```yaml
type: FullSlide
key: df02059657
```

`@part1`
```py
import numpy as np
np_city = ... # Implementation left out
np_city
```{{1}}

```out
array([[1.64, 71.78],
       [1.37, 63.35],
       [1.6 , 55.09],
       ...,
       [2.04, 74.85],
       [2.04, 68.72],
       [2.01, 73.57]])
```{{1}}

`@script`
Imaginez que vous meniez une enquête à l'échelle d'une ville dans laquelle vous interrogez 5 000 adultes sur leur taille et leur poids. Vous obtenez alors quelque chose comme ceci : un tableau numpy 2D, que j'ai nommé np tiret city, qui comporte 5 000 lignes, correspondant aux 5 000 personnes, et deux colonnes, correspondant à la taille et au poids.

Se contenter de fixer ces chiffres comme un robot ne vous apportera aucune information utile. Ce que vous pouvez faire, cependant, c'est générer des statistiques récapitulatives sur vos données.


---

## NumPy

```yaml
type: FullSlide
key: d3c991b91f
code_zoom: 90
```

`@part1`
```py
np.mean(np_city[:, 0])
```{{1}}

```out
1.7472
```{{1}}

```py
np.median(np_city[:, 0])
```{{2}}

```out
1.75
```{{2}}

`@script`
Outre une structure de données efficace pour le traitement intensif de données, NumPy s'avère également performant pour ce type de tâches.

Pour commencer, vous pouvez essayer de déterminer la taille moyenne de ces 5 000 personnes à l'aide de la fonction MEAN de NumPy. Comme il s'agit d'une fonction du paquet NumPy, veuillez vous assurer de commencer par np..

Bien entendu, j'ai d'abord dû effectuer une opération de sous-ensemble pour extraire la colonne « taille » du tableau 2D. Il semble qu'en moyenne, les personnes mesurent 1 mètre 75. Quelle est la taille moyenne ? Il s'agit de la taille de la personne du milieu si vous classez toutes les personnes de la plus petite à la plus grande. Au lieu d'écrire un code Python complexe pour déterminer cela, vous pouvez simplement utiliser la fonction median de NumPy :

Vous pouvez effectuer des opérations similaires pour la colonne « poids » dans np tiret city. Souvent, ces statistiques récapitulatives vous permettront de vérifier la cohérence de vos données. Si vous obtenez un poids moyen de 2 000 kilos, il est fort probable que vos mesures soient incorrectes.

Outre la moyenne et la médiane, il existe d'autres fonctions,


---

## NumPy

```yaml
type: FullSlide
key: a66131c711
```

`@part1`
```py
np.corrcoef(np_city[:, 0], np_city[:, 1])
```

```out
array([[ 1.     , -0.01802],
       [-0.01803,  1.     ]])
```

```py
np.std(np_city[:, 0])
```{{1}}

```out
0.1992
```{{1}}

- sum(), sort(), ...{{2}}

- Appliquez un type de données unique : rapidité !{{3}}

`@script`
comme corrcoeff pour vérifier si, par exemple, la taille et le poids sont corrélés,

et std, pour l'écart type.

NumPy propose également des fonctions plus basiques, telles que sum et sorte, qui existent également dans la distribution Python de base. Cependant, la grande différence réside ici dans la rapidité. NumPy impose un type de données unique dans un tableau, ce qui permet d'accélérer considérablement les calculs.


---

## Générer des données

```yaml
type: FullSlide
key: 0c27803967
code_zoom: 80
```

`@part1`
- Arguments pour `np.random.normal()` {{1}}
	- moyenne de distribution{{1}}
    - écart type de distribution{{1}}
    - nombre d'échantillons{{1}}

```py
height = np.round(np.random.normal(1.75, 0.20, 5000), 2)

weight = np.round(np.random.normal(60.32, 15, 5000), 2)

```{{1}}
```py
np_city = np.column_stack((height, weight))
```{{2}}

`@script`
À titre d'information, si vous vous demandez comment j'ai obtenu les données présentées dans cette vidéo : Nous les avons simulées avec les fonctions NumPy. J'ai échantillonné deux distributions aléatoires 5 000 fois pour créer les tableaux de taille et de poids, puis j'ai utilisé column tiret stack pour les assembler en deux colonnes. Encore une fonctionnalité remarquable de NumPy !

Un autre excellent outil pour mieux comprendre vos données consiste à les visualiser, mais cela fera l'objet du prochain cours.


---

## Passons à la pratique !

```yaml
type: FinalSlide
key: c4df18cfc1
```

`@script`
Commencez par consulter les exercices pour apprendre à explorer vos tableaux NumPy.
