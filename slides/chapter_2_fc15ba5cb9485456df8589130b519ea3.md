---
title: Insert title here
key: fc15ba5cb9485456df8589130b519ea3
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/zh-CN/ec82c8af-761f-4eba-8758-c6d240e012a1-c7c53b322b936e20be2d0f2fe6271982.mp3
---

## 获取列表子集

```yaml
type: TitleSlide
key: e4c1e2cc21
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
创建好自己的 Python 列表之后，你需要了解如何访问列表中的信息。

---

## 获取列表子集

```yaml
type: FullSlide
key: 3c299aff4c
code_zoom: 70
```

`@part1`
```py
fam = ["liz", 1.73, "emma", 1.68, "mom", 1.71, "dad", 1.89]
fam
```{{1}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```{{1}}

```py
fam[3]
```{{2}}

```out
1.68
```{{2}}

`@script`
Python 是通过“索引”来实现这一点的。我们再来看看这个 fam 列表：列表中第 1 个元素的索引是 0，第 2 个是 1，以此类推。假设你想把 emma 的身高提取出来，也就是浮点数 1.68。因为它是第 4 个元素，对应的索引就是 3，因此，只需在方括号里写上 3 就能将其选中。

同理，如果要选择字符串 "dad"，

---

## 获取列表子集

```yaml
type: FullSlide
key: e036a40a08
code_zoom: 70
```

`@part1`
```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam[6]
```{{1}}

```out
'dad'
```{{1}}

```py
fam[-1]
```{{2}}

```out
1.89
```{{2}}

```py
fam[7]
```{{3}}

```out
1.89
```{{3}}

`@script`
它是列表中的第 7 个元素，只需在方括号里填入索引 6 就行。

你还可以使用负数索引来倒着数，当你想获取列表末尾的数据时，这非常方便。比如，你想直接拿到爸爸的身高，使用索引 -1 就行。该列表中所有元素对应的负数索引如下。

---

## 获取列表子集

```yaml
type: FullSlide
key: 06e85623c2
disable_transition: true
code_zoom: 70
```

`@part1`
```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam[6]
```

```out
'dad'
```

```py
fam[-1]  # <-
```

```out
1.89
```

```py
fam[7] # <-
```

```out
1.89
```

`@script`
这意味着这两行代码返回的结果是完全一样的。

除了单元素索引，Python 还支持“切片”，

---

## 列表切片

```yaml
type: FullSlide
key: 125c4cb6c9
code_zoom: 70
```

`@part1`
```py
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam[3:5]
```{{1}}

```out
[1.68, 'mom']
```{{2}}

```py
fam[1:4]
```{{4}}

```out
[1.73, 'emma', 1.68]
```{{5}}

![Python 列表中切片语法的示意图，展示了包含起始值但不包含结束值的切片规则。](https://assets.datacamp.com/img/translations/zh-CN/q1/production/repositories/288/datasets/83dd2f807be0d4d08a187935eed11667c18fcfe3/slicing-syntax.jpg = 40){{3}}

`@script`
它可以让你一次性选出多个元素，并创建一个新的列表。你可以用冒号指定一个范围来实现切片。我们先看一下列表，然后试一下这段代码。

你能猜出它会返回什么吗？也许是一个包含浮点数 1.68、字符串 "mom" 和浮点数 1.71 的列表，正好对应第 4、第 5 和第 6 个元素？我们一起来看看实际输出：

结果只有索引为 3 和 4 的元素被选出来了，索引为 5 的元素并不包含在内。切片的基本语法规则是：冒号前面的索引，也就是起始位置，是包含在内的；冒号后面的索引，也就是结束位置，不会包含在内，这就是所谓的“左闭右开”。

基于这条规则，你能猜出下面这行代码会返回什么吗？

你肯定猜对了，它会返回一个包含 3 个元素的列表，分别对应 fam 列表中索引为 1、2 和 3 的数据。

另外，你也可以选择省略冒号前面或后面的索引。

---

## 列表切片

```yaml
type: FullSlide
key: 8207b3255e
disable_transition: true
code_zoom: 70
```

`@part1`
```py
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam[:4]
```{{1}}

```out
['liz', 1.73, 'emma', 1.68]
```{{1}}

```py
fam[5:]
```{{2}}

```out
[1.71, 'dad', 1.89]
```{{2}}

`@script`
如果省略切片开始位置的起始索引，Python 默认会从索引 0 开始切片，比如这个例子。

如果省略结束索引，则会一直切片到列表的最后一个元素，包括末尾元素，就像这样。

现在，快去做一做练习吧！

---

##   动手练一练！
```yaml
type: FinalSlide
key: 048b2b774f
```

`@script`
你将继续使用之前亲自创建的列表，运用各种切片和提取技巧，精准获取你想要的信息！
