---
title: Insert title here
key: 355ed52d2fb0d67508c6a311b7cbc6d3
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/ko-KR/ec4edfb2-0dec-46ff-973a-20b5713aaf08.mp3
---

## 리스트 조작

```yaml
type: TitleSlide
key: 6484e4d1f6
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
와, 정말 잘하고 계시네요. 이제 생성과 부분 선택을 살펴봤으니, 파이썬 리스트 퍼즐의 마지막 조각은

---

## 리스트 조작

```yaml
type: FullSlide
key: 5b83249ee9
```

`@part1`
- 리스트 요소 변경{{1}}

- 리스트 요소 추가{{2}}

- 리스트 요소 제거{{3}}

`@script`
조작, 즉 리스트의 요소를 바꾸거나 요소를 추가하고 제거하는 방법입니다.

---

## 리스트 요소 변경

```yaml
type: FullSlide
key: c1d58a3c4c
code_zoom: 64
```

`@part1`
```py
fam = ["liz", 1.73, "emma", 1.68, "mom", 1.71, "dad", 1.89]
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam[7] = 1.86
fam
```{{1}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.86]
```{{1}}

```py
fam[0:2] = ["lisa", 1.74]
fam
```{{2}}

```out
['lisa', 1.74, 'emma', 1.68, 'mom', 1.71, 'dad', 1.86]
```{{2}}

`@script`
리스트 요소를 바꾸는 건 꽤 간단합니다. 리스트를 부분 선택할 때 썼던 대괄호를 그대로 사용하고, 등호로 새 값을 할당하면 됩니다. fam을 다시 보니 아버지 키 정보가 더 이상 최신이 아니라고 합시다. 나이가 들면서 조금 줄어들어 1.89미터가 아니라 1.86미터여야 하네요. 인덱스 7에 있는 이 리스트 요소를 바꾸려면, 다음 코드 한 줄이면 됩니다.

이제 fam을 확인해 보면 값이 업데이트된 것을 볼 수 있습니다.

리스트의 한 구간을 통째로 바꾸는 것도 가능합니다. "liz"와 1.73을 바꾸려면 0:2로 처음 두 요소에 접근하고, 그 자리에 새 리스트를 할당하면 됩니다.

문자열과 정수에서 플러스 연산자가 어떻게 달랐는지 기억하시나요?

---

## 요소 추가 및 제거

```yaml
type: FullSlide
key: a66d56cb46
code_zoom: 74
```

`@part1`
```py
fam + ["me", 1.79]
```{{1}}

```out
['lisa', 1.74,'emma', 1.68, 'mom', 1.71, 'dad', 1.86, 'me', 1.79]
```{{1}}

```py
fam_ext = fam + ["me", 1.79]
```{{2}}
```py
del fam[2]
```{{3}}
```py
fam
```{{4}}

```out
['lisa', 1.74, 1.68, 'mom', 1.71, 'dad', 1.86]
```{{4}}

`@script`
리스트에서도 마찬가지로 다르게 동작합니다. 두 리스트에 플러스 기호를 쓰면, 파이썬은 두 리스트의 내용을 하나로 이어 붙입니다. fam 키 리스트에 본인 이름과 키를 추가하고 싶다면, 이렇게 하면 됩니다.

물론 이 새로운 리스트를 fam_ext 같은 변수에 저장할 수도 있습니다.

마지막으로, 리스트에서 요소를 삭제하는 것도 아주 간단합니다. 여기서는 del을 사용하면 됩니다. 예를 들어, 인덱스 2에 있는 요소, 즉 "emma"를 리스트에서 지우는 다음 한 줄을 보세요.

지금 fam을 확인해 보면 "emma" 문자열이 사라졌습니다. 인덱스를 하나 제거했기 때문에, "emma" 뒤에 있던 모든 요소의 인덱스가 하나씩 앞으로 당겨졌습니다. 같은 줄을 다시 실행하면, 또다시 인덱스 2의 요소를 지우게 되고, 이번에는 emma의 키인 1.68미터가 삭제됩니다.

파이썬 리스트가 실제로 어떻게 동작하는지

---

## 이면에서 (1)

```yaml
type: TwoColumns
key: ef5370967a
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```{{1}}

`@part2`
![장면 뒤에서 (1)](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/e91761036b6647fa635fe8493b4ff3379587f5d5/ch_2_3_slides.024.png = 70){{2}}

`@script`
내부에서 무슨 일이 일어나는지를 이제 이해하는 게 중요해집니다. 이런 식으로 새 리스트 x를 만들면 실제로는 무슨 일이 일어날까요?

간단히 말하면, 컴퓨터 메모리에 리스트를 저장하고, 그 리스트의 '주소', 즉

---

## 이면에서 (1)

```yaml
type: TwoColumns
key: 4d48163f25
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = x
```{{1}}
```py
y[1] = "z"
y
```{{2}}

```out
['a', 'z', 'c']
```{{2}}

```py
x
```{{3}}

```out
['a', 'z', 'c']
```{{3}}

`@part2`
![장면 뒤에서 (1)](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/03d95d40b2e0d631ea89f07cadf12e66babd3693/ch_2_3_slides.025.png = 70)

`@script`
리스트가 메모리 어디에 있는지를 x에 저장하는 것입니다. 즉 x 자체가 리스트의 모든 요소를 담는 게 아니라, 리스트에 대한 참조를 담습니다. 기본적인 연산에서는 이 차이가 크게 중요하지 않지만, 리스트를 복사하기 시작하면 중요해집니다. 예를 들어 설명해 보겠습니다.

등호만 사용해서 리스트 x를 새 변수 y에 저장해 보죠.

이제 y 리스트에서 인덱스 1의 요소를 이렇게 바꿔 보겠습니다.

신기하게도, 다시 x를 확인하면 여기에서도 두 번째 요소가 바뀐 것을 볼 수 있습니다.

이는 등호로 x를 y에 복사했을 때,

---

## 이면에서 (1)

```yaml
type: TwoColumns
key: 4a5827f664
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = x
```
```py
y[1] = "z"
y
```

```out
['a', 'z', 'c']
```

```py
x
```

```out
['a', 'z', 'c']
```

`@part2`
![장면 뒤에서 (1)](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/cee01ad8680d8cd824bab998aed4c5e5f74521bb/ch_2_3_slides.030.png = 70)

`@script`
실제 값이 아니라 리스트에 대한 참조를 복사했기 때문입니다.

---

## 이면에서 (1)

```yaml
type: TwoColumns
key: ef3476e2fc
disable_transition: true
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = x
```
```py
y[1] = "z"
y
```

```out
['a', 'z', 'c']
```

```py
x
```

```out
['a', 'z', 'c']
```

`@part2`
![장면 뒤에서 (1)](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/fff4d255ec69a9a6e4d64394bdb92464390498c4/ch_2_3_slides.031.png = 70)

`@script`
리스트의 요소를 업데이트하면, 컴퓨터 메모리 안의 동일한 하나의 리스트를 변경하는 것입니다. x와 y가 모두 그 리스트를 가리키기 때문에, 두 변수에서 모두 변경 사항이 보입니다.

같은 값을 가지지만 메모리에서 새로운 리스트를 가리키는 y를 만들고 싶다면,

---

## 이면에서 (2)

```yaml
type: TwoColumns
key: 05f37e881d
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```

`@part2`
![장면 뒤에서 (2)](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/97dc873ce995a7fb3cf83305c56a6a9b4f23de51/ch_2_3_slides.033.png)

`@script`
등호 말고 다른 방법을 써야 합니다. list 함수나,

---

## 이면에서 (2)

```yaml
type: TwoColumns
key: 678dfa958a
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = list(x)
y = x[:]
```

`@part2`
![장면 뒤에서 (2)](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/ec9a50129117c16795d74c53b070c34c0015f6d1/ch_2_3_slides.034.png)

`@script`
이렇게 사용할 수 있고, 혹은 슬라이싱으로 모든 리스트 요소를 명시적으로 선택할 수도 있습니다.

이제

---

## 이면에서 (2)

```yaml
type: TwoColumns
key: d211be5714
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = list(x)
y = x[:]
```
```py
y[1] = "z"
x
```

```out
['a', 'b', 'c']
```

`@part2`
![장면 뒤에서 (2)](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/3f6b4d36a70007385ff752d07fa842a1e3a7f878/ch_2_3_slides.036.png)

`@script`
y가 가리키는 리스트를 변경해도, x에는 영향이 없습니다.

조금 어렵게 느껴지셨다면 걱정하지 않으셔도 됩니다.

---

## 연습해 봅시다!

```yaml
type: FinalSlide
key: 934a5be348
```

`@script`
연습 문제들이 리스트 조작과 리스트의 미묘한 내부 동작을 이해하는 데 큰 도움이 될 거예요. 분명 잘해내실 겁니다!
