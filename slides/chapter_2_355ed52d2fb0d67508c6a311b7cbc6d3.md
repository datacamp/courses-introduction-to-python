---
title: Insert title here
key: 355ed52d2fb0d67508c6a311b7cbc6d3
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/ja-JP/fc9bfbdf-500a-4773-8db0-0b55bd7fa1dd-7604e7226890d52dcd8723e3c534e4df.mp3
---

## リストの操作

```yaml
type: TitleSlide
key: 6484e4d1f6
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
ここまでとても順調です。それでは、リストの作成と要素の取り出しに続く、Pythonリストの最後のピースを見ていきましょう。

---

## リスト操作

```yaml
type: FullSlide
key: 5b83249ee9
```

`@part1`
- リスト要素を変更する{{1}}

- リスト要素を追加する{{2}}

- リスト要素を削除する{{3}}

`@script`
それはリストの操作です。つまり、リスト内の要素を変更したり、リストに要素を追加したり、リストから要素を削除したりする方法です。
---

## リストの要素を変更する

```yaml
type: FullSlide
key: c1d58a3c4c
code_zoom: 64
```

`@part1`
```py
fam = ["liz", 1.73, "emma", 1.68, "mom", 1.71, "dad", 1.89]
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam[7] = 1.86
fam
```{{1}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.86]
```{{1}}

```py
fam[0:2] = ["lisa", 1.74]
fam
```{{2}}

```out
['lisa', 1.74, 'emma', 1.68, 'mom', 1.71, 'dad', 1.86]
```{{2}}

`@script`
リストの要素を変更するのはとても簡単です。リストから要素を取り出すときに使ったのと同じ角括弧を使い、「イコール記号」で新しい要素を代入します。もう一度famを見てみましょう。お父さんの身長が最新ではなくなっていることに気づいたとします。年齢とともに少し身長が縮んだようです。1.89メートルではなく、1.86メートルにする必要があります。インデックス7にあるこのリスト要素を変更するには、次のコード行を使えます。

今famを確認すると、値が更新されているのがわかります。

リストのスライス全体を一度に変更することもできます。要素"liz"と1.73を変更するには、0:2で最初の2つの要素にアクセスし、新しいリストを代入します。

文字列と整数でプラス演算子の動作がどのように違っていたか、まだ覚えていますか？
---

## 要素の追加と削除

```yaml
type: FullSlide
key: a66d56cb46
code_zoom: 74
```

`@part1`
```py
fam + ["me", 1.79]
```{{1}}

```out
['lisa', 1.74,'emma', 1.68, 'mom', 1.71, 'dad', 1.86, 'me', 1.79]
```{{1}}

```py
fam_ext = fam + ["me", 1.79]
```{{2}}
```py
del fam[2]
```{{3}}
```py
fam
```{{4}}

```out
['lisa', 1.74, 1.68, 'mom', 1.71, 'dad', 1.86]
```{{4}}

`@script`
リストの場合は、また違った動作になります。プラス記号を2つのリストに使うと、Pythonはそれらの中身をつなげて1つのリストにします。自分の名前と身長をfamの身長リストに追加したいとしましょう。このコードでうまくいきます。

もちろん、この新しいリストを変数、たとえば fam_e-x-t に保存することもできます。

最後に、リストから要素を削除するのもとても簡単です。ここでは d-e-l を使う必要があります。 例えば、インデックス 2 の要素である "emma" リストから削除するには、次の行を使います。

ここで fam を確認すると、"emma" という文字列がなくなっているのがわかります。 インデックス2の要素を削除したため、"emma" の後にあったすべての要素のインデックスが1つずつ前にずれました。 同じ行をもう一度実行すると、再びインデックス 2 の要素、つまり今度はエマの身長である 1.68 メートルが削除されます。

Python リストが内部で実際にどのように機能しているのかを理解することが、ここからとても重要になります。

---

## 内部の仕組み（1）

```yaml
type: TwoColumns
key: ef5370967a
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```{{1}}

`@part2`
![ch_2_3_slides.024.png](https://assets.datacamp.com/img/translations/ja-JP/production/repositories/288/datasets/e91761036b6647fa635fe8493b4ff3379587f5d5/ch_2_3_slides.024.png = 70){{2}} 

`@script`
このように新しいリスト x を作成すると、実際には何が起こるのでしょうか？簡単に言うと、リストはコンピューターのメモリに保存され、その保存場所を示す情報も保存されます。

---

## 内部の仕組み（1）

```yaml
type: TwoColumns
key: 4d48163f25
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = x
```{{1}}
```py
y[1] = "z"
y
```{{2}}

```out
['a', 'z', 'c']
```{{2}}

```py
x
```{{3}}

```out
['a', 'z', 'c']
```{{3}}

`@part2`
![ch_2_3_slides.025.png](https://assets.datacamp.com/img/translations/ja-JP/production/repositories/288/datasets/03d95d40b2e0d631ea89f07cadf12e66babd3693/ch_2_3_slides.025.png = 70)

`@script`
つまり、リストがコンピューターのメモリ内のどこにあるかを示す情報が、x に保存されます。x には実際にはリストのすべての要素が入っているのではなく、リストへの参照が入っています。基本的な操作では、この違いはそれほど重要ではありませんが、リストをコピーし始めると重要になってきます。例を使って説明しましょう。

「イコール記号」を使って、リスト x を新しい変数 y に代入してみましょう。

では、リスト y のインデックス 1 の要素をこのように変更してみましょう。

面白いことに、ここで x をもう一度確認すると、x でも 2 つ目の要素が変更されています。

それは、「イコール記号」を使って x を y にコピーしたとき、
---

## 内部の仕組み（1）

```yaml
type: TwoColumns
key: 4a5827f664
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = x
```
```py
y[1] = "z"
y
```

```out
['a', 'z', 'c']
```

```py
x
```

```out
['a', 'z', 'c']
```

`@part2`
![ch_2_3_slides.030.png](https://assets.datacamp.com/img/translations/ja-JP/production/repositories/288/datasets/cee01ad8680d8cd824bab998aed4c5e5f74521bb/ch_2_3_slides.030.png = 70)

`@script`
リストへの参照をコピーしただけで、実際の値自体はコピーしていません。
---

## 内部の仕組み（1）

```yaml
type: TwoColumns
key: ef3476e2fc
disable_transition: true
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = x
```
```py
y[1] = "z"
y
```

```out
['a', 'z', 'c']
```

```py
x
```

```out
['a', 'z', 'c']
```

`@part2`
![ch_2_3_slides.031.png](https://assets.datacamp.com/img/translations/ja-JP/production/repositories/288/datasets/fff4d255ec69a9a6e4d64394bdb92464390498c4/ch_2_3_slides.031.png = 70)

`@script`
リストの要素を更新するときは、コンピューターのメモリ内にある同じ1つのリストを変更していることになります。 x も y もこのリストを参照しているので、どちらの変数からも更新が見えます。

同じ値を持つ新しいリストをメモリ内に作成し、それを参照する y を作りたい場合は、
---

## 内部の仕組み（2）

```yaml
type: TwoColumns
key: 05f37e881d
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```

`@part2`
![ch_2_3_slides.033.png](https://assets.datacamp.com/img/translations/ja-JP/production/repositories/288/datasets/97dc873ce995a7fb3cf83305c56a6a9b4f23de51/ch_2_3_slides.033.png)

`@script`
イコール記号以外のものを使う必要があります。 list 関数を使うことができます。

---

## 内部の仕組み（2）

```yaml
type: TwoColumns
key: 678dfa958a
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = list(x)
y = x[:]
```

`@part2`
![ch_2_3_slides.034.png](https://assets.datacamp.com/img/translations/ja-JP/production/repositories/288/datasets/ec9a50129117c16795d74c53b070c34c0015f6d1/ch_2_3_slides.034.png)

`@script`
このようにするか、またはスライスを使ってリストのすべての要素を明示的に取り出します。

もし今、
---

## 内部の仕組み（2）

```yaml
type: TwoColumns
key: d211be5714
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = list(x)
y = x[:]
```
```py
y[1] = "z"
x
```

```out
['a', 'b', 'c']
```

`@part2`
![ch_2_3_slides.036.png](https://assets.datacamp.com/img/translations/ja-JP/production/repositories/288/datasets/3f6b4d36a70007385ff752d07fa842a1e3a7f878/ch_2_3_slides.036.png)

`@script`
y が指しているリストを変更しても、x には影響しません。

もしこれが少し難しく感じたとしても、心配しないでください。

---

## 練習しましょう！
```yaml
type: FinalSlide
key: 934a5be348
```

`@script`
演習を通じて、リストの操作や、リストの少し複雑な内部構造を理解できるようになります。きっとうまくいきます！
