---
title: Insert title here
key: 355ed52d2fb0d67508c6a311b7cbc6d3
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/zh-CN/6e7e9cca-c0ef-4b8e-96e0-67c0fd1d98f7-2cfdd7a1756aa3dd2dfa424533813504.mp3
---

## 操作列表

```yaml
type: TitleSlide
key: 6484e4d1f6
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
太棒了！完成子集的创建和提取之后，Python 列表这块拼图的最后一片就是

---

## 列表操作

```yaml
type: FullSlide
key: 5b83249ee9
```

`@part1`
- 更改列表元素{{1}}

- 添加列表元素{{2}}

- 删除列表元素{{3}}

`@script`
“操作”，也就是如何更改列表里的元素，以及怎样往列表里添加或删除数据。

---

## 更改列表元素

```yaml
type: FullSlide
key: c1d58a3c4c
code_zoom: 64
```

`@part1`
```py
fam = ["liz", 1.73, "emma", 1.68, "mom", 1.71, "dad", 1.89]
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam[7] = 1.86
fam
```{{1}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.86]
```{{1}}

```py
fam[0:2] = ["lisa", 1.74]
fam
```{{2}}

```out
['lisa', 1.74, 'emma', 1.68, 'mom', 1.71, 'dad', 1.86]
```{{2}}

`@script`
修改列表里的元素非常简单。用之前提取子集时用到的方括号选中目标，再用等号赋一个新值就完成了。假设在重新检查 fam 列表时，你发现爸爸的身高数据过时了，毕竟人年纪大了都会“有点缩水”，因此，你需要把他的身高从 1.89 米改成 1.86 米。那么，要修改索引为 7 的这个元素，只需这行代码就能实现。

此时再打印 fam，数值就已经更新了。

你甚至能一次性替换一整段切片。比如，你想把 "liz" 和 1.73 一起改掉，用 0:2 选中前两个元素，然后直接赋给它一个新的列表。

对了，你还记得对字符串和整数使用加号有什么区别吗？

---

## 添加和移除元素

```yaml
type: FullSlide
key: a66d56cb46
code_zoom: 74
```

`@part1`
```py
fam + ["me", 1.79]
```{{1}}

```out
['lisa', 1.74,'emma', 1.68, 'mom', 1.71, 'dad', 1.86, 'me', 1.79]
```{{1}}

```py
fam_ext = fam + ["me", 1.79]
```{{2}}
```py
del fam[2]
```{{3}}
```py
fam
```{{4}}

```out
['lisa', 1.74, 1.68, 'mom', 1.71, 'dad', 1.86]
```{{4}}

`@script`
对于列表，它的作用又是不同的。如果你把两个列表加在一起，Python 会直接把它们的内容拼接成一个大列表。比如你想把自己的名字和身高也加进 fam 列表里，这么写就能实现。

当然，你也可以把这个新列表存进一个变量里，比如 fam_ext。

最后，删掉列表里的元素同样简单，用 del 关键字就行。比如这行代码删除了索引为 2 的元素，也就是 "emma"。

再看一下 fam，字符串 "emma" 就已经彻底消失了。因为删掉了一个位置，排在 "emma" 后面的所有元素都会自动向前挪一位。要是你再运行一次完全相同的代码，删掉的依然是索引 2 位置上的元素 — 不过这次对应的就是 emma 的身高 1.68 米了。

学到这里，弄清楚 Python 列表在底层

---

## 底层原理揭秘 (1)

```yaml
type: TwoColumns
key: ef5370967a
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```{{1}}

`@part2`
![ch_2_3_slides.024.png](https://assets.datacamp.com/production/repositories/288/datasets/e91761036b6647fa635fe8493b4ff3379587f5d5/ch_2_3_slides.024.png = 70){{2}}

`@script`
是怎么运行的，就变得非常关键了。当你像这样新建一个列表 x 时，底层到底发生了什么？

简单来说，你是在计算机内存里存放了一个列表，然后把这个列表的“内存地址”赋给了 x，所谓“内存地址”，

---

## 底层原理揭秘 (1)

```yaml
type: TwoColumns
key: 4d48163f25
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = x
```{{1}}
```py
y[1] = "z"
y
```{{2}}

```out
['a', 'z', 'c']
```{{2}}

```py
x
```{{3}}

```out
['a', 'z', 'c']
```{{3}}

`@part2`
![ch_2_3_slides.025.png](https://assets.datacamp.com/production/repositories/288/datasets/03d95d40b2e0d631ea89f07cadf12e66babd3693/ch_2_3_slides.025.png = 70)

`@script`
就是列表在计算机内存中的存储位置。这意味着 x 里面装的其实不是那些数据本身，而是一个指向该列表的“指针引用”。在做基本操作时，这一区别并不明显，但只要一涉及“复制列表”，差异就大了。我举个例子你就明白了：

假设我们直接用等号，把列表 x 赋值给一个新变量 y。

接着，我们把列表 y 里索引为 1 的元素改掉。

神奇的事情发生了 — 如果你再去打印 x，会发现 x 里的第二个元素竟然也跟着一起变了！

这是因为用等号把 x 赋值给 y 时，

---

## 底层原理揭秘 (1)

```yaml
type: TwoColumns
key: 4a5827f664
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = x
```
```py
y[1] = "z"
y
```

```out
['a', 'z', 'c']
```

```py
x
```

```out
['a', 'z', 'c']
```

`@part2`
![ch_2_3_slides.030.png](https://assets.datacamp.com/img/translations/zh-CN/production/repositories/288/datasets/cee01ad8680d8cd824bab998aed4c5e5f74521bb/ch_2_3_slides.030.jpg = 70)

`@script`
你复制的只是指向列表的引用，而不是那些实际的数据值。

---

## 底层原理揭秘 (1)

```yaml
type: TwoColumns
key: ef3476e2fc
disable_transition: true
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = x
```
```py
y[1] = "z"
y
```

```out
['a', 'z', 'c']
```

```py
x
```

```out
['a', 'z', 'c']
```

`@part2`
![ch_2_3_slides.031.png](https://assets.datacamp.com/img/translations/zh-CN/production/repositories/288/datasets/fff4d255ec69a9a6e4d64394bdb92464390498c4/ch_2_3_slides.031.jpg = 70)

`@script`
所以当你修改列表元素时，改动的其实是计算机内存里的同一个列表。因为 x 和 y 指向同一个地方，所以无论通过哪个变量查看，数据都变了。

如果你想创建一个指向内存中存储相同数值的新列表 y，

---

## 底层原理揭秘 (2)

```yaml
type: TwoColumns
key: 05f37e881d
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```

`@part2`
![ch_2_3_slides.033.png](https://assets.datacamp.com/img/translations/zh-CN/production/repositories/288/datasets/97dc873ce995a7fb3cf83305c56a6a9b4f23de51/ch_2_3_slides.033.jpg)

`@script`
就不能只用等号。你可以像这样调用 list() 函数，

---

## 底层原理揭秘 (2)

```yaml
type: TwoColumns
key: 678dfa958a
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = list(x)
y = x[:]
```

`@part2`
![ch_2_3_slides.034.png](https://assets.datacamp.com/production/repositories/288/datasets/ec9a50129117c16795d74c53b070c34c0015f6d1/ch_2_3_slides.034.png)

`@script`
或者用切片显式地把所有元素全选中，以便复制过去。

如果你现在

---

## 底层原理揭秘 (2)

```yaml
type: TwoColumns
key: d211be5714
disable_transition: true
code_zoom: 100
```

`@part1`
```py
x = ["a", "b", "c"]
```
```py
y = list(x)
y = x[:]
```
```py
y[1] = "z"
x
```

```out
['a', 'b', 'c']
```

`@part2`
![ch_2_3_slides.036.png](https://assets.datacamp.com/img/translations/zh-CN/production/repositories/288/datasets/3f6b4d36a70007385ff752d07fa842a1e3a7f878/ch_2_3_slides.036.jpg)

`@script`
再去改动 y 指向的列表，x 就完全不会受到影响。

如果刚才这些概念听起来有点烧脑，别担心！

---

##   动手练一练！
```yaml
type: FinalSlide
key: 934a5be348
```

`@script`
接下来的练习会帮你轻松掌握列表的操作技巧，理解它的底层运作细节。相信你一定能轻松掌握！
