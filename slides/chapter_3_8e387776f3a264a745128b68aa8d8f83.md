---
title: Insert title here
key: 8e387776f3a264a745128b68aa8d8f83
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/ja-JP/2e83711d-49b3-4a67-a47d-6803a376bcd1-3819455098bf05f3bf5a97a815a2f754-2.mp3
---

## メソッド

```yaml
type: TitleSlide
key: c536df1034
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
組み込み関数は
---

## 組み込み関数

```yaml
type: FullSlide
key: 45877294bd
```

`@part1`
- リストの最大値: max(){{1}}

- リストや文字列の長さ: len(){{2}}

- リスト内のインデックスを取得する: ?{{3}}

- リストを逆順にする: ?{{4}}

`@script`
Pythonの一部にすぎません。 リストの最大値を取り出す max や、リストや文字列の長さを調べる len などの関数については、すでに知っていますね。 では、リスト内の特定の要素のインデックスを調べたり、リストを逆順にしたりといった、他の基本的な操作についてはどうでしょうか？こうしたことを行う組み込み関数をどれだけ探しても、見つかりません。

---

## 基本に戻る

```yaml
type: TwoColumns
key: a3e45f6524
code_zoom: 75
```

`@part1`
 

```py
sister = "liz"
```

```py
height = 1.73
```{{1}}


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```{{2}}

`@part2`
![ch_3_2_slides.020.png](https://assets.datacamp.com/img/translations/ja-JP/production/repositories/288/datasets/c7a9757fa49f8396eb025ef221823441d6e66ced/ch_3_2_slides.020.png = 85){{3}} 

`@script`
これまでの演習で、すでにたくさんの変数を作成しました。 これまでに、ここにあるような文字列、float 型の値、リストなど、さまざまなPythonの型を扱ってきました。 これらの値やデータ構造はそれぞれ、いわゆるPythonのオブジェクトです。 この文字列はオブジェクトです。この float 型の値もオブジェクトです。そして、このリストも、そう、オブジェクトです。 これらのオブジェクトには、すでに見たことのある特定の型があります。

---

## 基本に戻る

```yaml
type: TwoColumns
key: 6a5eddd6ea
disable_transition: true
code_zoom: 75
```

`@part1`
 

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- メソッド: Functions that belong to objects{{1}}  

`@part2`
![ch_3_2_slides.024.png](https://assets.datacamp.com/img/translations/ja-JP/q1/production/repositories/288/datasets/6d444348823438f856363d02d093318f2ed457a3/ch_3_2_slides.024.png = 85)

`@script`
文字列、float、リストです。そしてもちろん、これらは "liz"、1.73、リスト全体といった、あなたが与えた値を表します。 しかし、それに加えて、Pythonのオブジェクトには「メソッド」と呼ばれるものもたくさん備わっています。 メソッドは、Pythonのオブジェクトに「属している」関数だと考えることができます。 s-t-r 型のPythonオブジェクトには、メソッドがあります。

---

## 基本に戻る

```yaml
type: TwoColumns
key: ff540e522c
disable_transition: true
code_zoom: 75
```

`@part1`
 

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- メソッド: オブジェクトに属する関数

`@part2`
![ch_3_2_slides.028.png](https://assets.datacamp.com/img/translations/ja-JP/production/repositories/288/datasets/80891dbbb1a9b4f759540c5d601cbfb661a894d9/ch_3_2_slides.028.png = 85)

`@script`
たとえば capitalize や replace のようなメソッドがありますが、float 型や list 型のオブジェクトにも、型に応じた固有のメソッドがあります。

理論はこれくらいにして、実際にメソッドを使ってみましょう！
---

## リストのメソッド

```yaml
type: FullSlide
key: 431cae8707
code_zoom: 85
```

`@part1`
```py
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam.index("mom") # "Call method index() on fam"
```{{1}}

```out
4
```{{2}}

```py
fam.count(1.73)
```{{3}}

```out
1
```{{4}}

`@script`
例えば、fam リストの中で文字列 "mom" のインデックスを調べたいとします。fam は list 型のPythonオブジェクトで、index という名前のメソッドを持っています。 メソッドを呼び出すには、次のようにドット記法を使います。 入力は、インデックスを調べたい要素である文字列 "mom" だけです。

Pythonは 4 を返します。これは確かに文字列 "mom" のインデックスです。 ここでは、fam リストに対して index メソッドを呼び出し、出力は 4 でした。 同様に、fam リストに対して count メソッドを使うことで、1.73 がリスト内に出現する回数を数えることができます。

Pythonは 1 を返します。これは納得できますね。身長が 1.73 メートルなのはリズだけだからです。

しかし、メソッドを持つPythonオブジェクトはリストだけではありません。 float 型、int 型、bool 型、s-t-r 型も

---

## 文字列（str）のメソッド

```yaml
type: FullSlide
key: 73c6a6ff3a
code_zoom: 80
```

`@part1`
```py
sister
```{{1}}

```out
'liz'
```{{1}}

```py
sister.capitalize()
```{{2}}

```out
'Liz'
```{{3}}

```py
sister.replace("z", "sa")
```{{4}}

```out
'lisa'
```{{5}}

`@script`
それぞれに固有のメソッドを持つPythonオブジェクトです。 例えば、文字列を表す変数 sister を見てみましょう。

sister に対して、入力なしで capitalize メソッドを呼び出すことができます。 最初の文字が大文字になった文字列を返します。

では、文字列の一部を別の文字列に置き換えたい場合はどうでしょうか？問題ありません。 sister に対して、2つの適切な入力を使って replace メソッドを呼び出すだけです。

出力では、"z" が "s-a" に置き換えられています。

---

## メソッド

```yaml
type: FullSlide
key: 346697c688
code_zoom: 80
```

`@part1`
- すべてのもの = オブジェクト{{1}}

- オブジェクトには、型に応じたメソッドがある{{2}}

```py
sister.replace("z", "sa")
```{{3}}

```out
'lisa'
```{{3}}

```py
fam.replace("mom", "mommy")
```{{4}}

```out
AttributeError: 'list' object has no attribute 'replace'
```{{4}}

`@script`
簡潔に言うと、Pythonではすべてがオブジェクトであり、それぞれのオブジェクトには特定のメソッドがあります。 オブジェクトの型、たとえば list 型、s-t-r 型、float 型などによって、利用できるメソッドは異なります。 sister のような文字列オブジェクトには replace メソッドがありますが、fam のようなリストにはこのメソッドがありません。このエラーからもわかります。

---

## メソッド

```yaml
type: FullSlide
key: c0100c8d69
disable_transition: true
code_zoom: 80
```

`@part1`
```py
sister.index("z")
```{{1}}

```out
2
```{{1}}

```py
fam.index("mom")
```{{1}}

```out
4
```{{1}}

`@script`
異なる型のオブジェクトが同じ名前のメソッドを持つことがあります。 index メソッドを例に見てみましょう。 これは、文字列とリストの両方で利用できます。 文字列に対して呼び出すと、文字列内の文字のインデックスが得られます。リストに対して呼び出すと、リスト内の要素のインデックスが得られます。 これは、オブジェクトの型によってメソッドの動作が異なることを意味します。

メソッドの演習に進む前に、もう1つお伝えしたいことがあります。

---

## メソッド（2）

```yaml
type: FullSlide
key: f03ac21e34
code_zoom: 75
```

`@part1`
```py
fam
```{{1}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```{{1}}

```py
fam.append("me")
```{{2}}
```py
fam
```{{3}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me']
```{{3}}

```py
fam.append(1.79)
fam
```{{4}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me', 1.79]
```{{4}}

`@script`
一部のメソッドは、呼び出し元のオブジェクト自体を変更することがあります。もう一度 fam リストを使って、それに対して append() メソッドを呼び出してみましょう。入力として、リストに追加したい文字列を渡します。

Pythonは出力を生成しませんが、fam リストをもう一度確認すると、文字列 "me" が追加されていることがわかります。

もう一度やってみましょう。今度は私の身長をリストに追加します。

再び、fam リストが拡張されました。

これはなかなか便利です。データ構造をその場で更新するコードをとても簡潔に書けますが、同時にかなり危険でもあります。メソッド呼び出しによっては、呼び出し元のオブジェクトを変更しないものもあれば、変更するものもあるので注意しましょう。

---

## 概要

```yaml
type: FullSlide
key: eecd826650
code_zoom: 80
```

`@part1`
関数{{1}}

```py
type(fam)
```{{2}}

```out
list
```{{2}}

メソッド：オブジェクトに対して関数を呼び出す{{3}}

```py
fam.index("dad")
```{{4}}

```out
6
```{{4}}

`@script`
ここで一度立ち止まって、内容をまとめましょう。type、max、round のようなPython関数は、このように呼び出すことができます。 また、Pythonオブジェクトに固有の関数であるメソッドもあります。 扱っているPythonオブジェクトの型によって、使用できるメソッドが異なり、その動作も異なります。 例えば、このようにドット記法を使って、オブジェクトに対してメソッドを呼び出すことができます。

Pythonのオブジェクト、メソッド、そしてPythonが内部でどのように動作しているかについては、まだまだ話すことがたくさんあります。

---

## 練習しましょう！
```yaml
type: FinalSlide
key: cefb86a284
```

`@script`
ただし、今はここまでにしておきましょう。さあ、演習に取り組んで、メソッドを新しいスキルとして使えるようにしていきましょう！
