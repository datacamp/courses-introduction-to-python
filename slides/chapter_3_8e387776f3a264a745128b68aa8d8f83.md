---
title: Insert title here
key: 8e387776f3a264a745128b68aa8d8f83
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/ko-KR/7c02fa8b-f831-4f4f-89c8-2658d628c67c.mp3
---

## 메서드

```yaml
type: TitleSlide
key: c536df1034
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
내장 함수는 파이썬 이야기의

---

## 내장 함수

```yaml
type: FullSlide
key: 45877294bd
```

`@part1`
- 리스트의 최댓값: max(){{1}}

- 리스트/문자열의 길이: len(){{2}}

- 리스트에서 인덱스 가져오기: ?{{3}}

- 리스트 뒤집기: ?{{4}}

`@script`
일부분일 뿐입니다. 이미 리스트의 최댓값을 구하는 max, 리스트나 문자열의 길이를 구하는 len 같은 함수는 알고 계시죠. 그럼 다른 기본적인 작업, 예를 들어 리스트에서 특정 요소의 인덱스를 찾거나 리스트를 뒤집는 건 어떨까요? 그런 기능을 하는 내장 함수를 아무리 찾아도, 찾을 수 없습니다.

---

## 기초 복습

```yaml
type: TwoColumns
key: a3e45f6524
code_zoom: 75
```

`@part1`
&nbsp;

```py
sister = "liz"
```

```py
height = 1.73
```{{1}}


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```{{2}}

`@part2`
![ch_3_2_slides.020.png](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/c7a9757fa49f8396eb025ef221823441d6e66ced/ch_3_2_slides.020.png = 85){{3}}

`@script`
지금까지의 연습에서 여러분은 이미 여러 변수를 만들었습니다. 문자열, 부동소수점 수, 리스트 같은 다양한 파이썬 타입을 생성해 보셨죠. 여기 보이는 것들처럼요. 이런 값이나 데이터 구조 각각은 파이썬 객체라고 부릅니다. 이 문자열도 객체이고, 이 float도 객체이며, 이 리스트도 맞습니다, 객체입니다. 이런 객체에는 여러분이 이미 아는 특정 타입이 있습니다:

---

## 기초 복습

```yaml
type: TwoColumns
key: 6a5eddd6ea
disable_transition: true
code_zoom: 75
```

`@part1`
&nbsp;

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- 메서드: 객체에 속한 함수{{1}}

`@part2`
![ch_3_2_slides.024.png](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/6d444348823438f856363d02d093318f2ed457a3/ch_3_2_slides.024.png = 85)

`@script`
string, float, list이고, 물론 "liz", 1.73, 그리고 하나의 전체 리스트처럼 여러분이 준 값을 표현합니다. 그런데 여기에 더해, 파이썬 객체에는 이른바 "메서드"도 여러 개 따라옵니다. 메서드는 파이썬 객체에 "속한" 함수라고 생각하시면 됩니다. 문자열 타입의 파이썬 객체에는 메서드가 있고,

---

## 기초 복습

```yaml
type: TwoColumns
key: ff540e522c
disable_transition: true
code_zoom: 75
```

`@part1`
&nbsp;

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- 메서드: 객체에 속한 함수

`@part2`
![ch_3_2_slides.028.png](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/80891dbbb1a9b4f759540c5d601cbfb661a894d9/ch_3_2_slides.028.png = 85)

`@script`
예를 들어 capitalize나 replace가 있습니다. 또한 float와 list 타입의 객체에도, 타입에 따라 고유한 메서드가 있습니다.

이론은 이 정도로 하고, 이제 메서드를 써 보겠습니다!

---

## list 메서드

```yaml
type: FullSlide
key: 431cae8707
code_zoom: 85
```

`@part1`
```py
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam.index("mom") # "fam에 대해 index() 메서드 호출"
```{{1}}

```out
4
```{{2}}

```py
fam.count(1.73)
```{{3}}

```out
1
```{{4}}

`@script`
fam 리스트에서 문자열 "mom"의 인덱스를 얻고 싶다고 해봅시다. fam은 list 타입의 파이썬 객체이고, index라는 메서드를 갖고 있습니다. 메서드를 호출하려면 이렇게 점 표기법을 사용합니다. 유일한 입력은 요소의 인덱스를 구하고 싶은 문자열 "mom"입니다.

파이썬은 4를 반환하는데, 실제로 문자열 "mom"의 인덱스가 4가 맞습니다. 여기서는 fam 리스트 "에 대해" index 메서드를 호출했고, 출력은 4였습니다. 비슷하게, fam 리스트에서 count 메서드를 사용해 1.73이 몇 번 등장하는지도 셀 수 있습니다.

파이썬은 1을 줍니다. 이해되시죠? 1.73미터인 사람은 liz 한 명뿐이니까요.

하지만 메서드가 있는 파이썬 객체는 리스트만이 아닙니다. float, 정수, 불리언, 문자열도

---

## str 메서드

```yaml
type: FullSlide
key: 73c6a6ff3a
code_zoom: 80
```

`@part1`
```py
sister
```{{1}}

```out
'liz'
```{{1}}

```py
sister.capitalize()
```{{2}}

```out
'Liz'
```{{3}}

```py
sister.replace("z", "sa")
```{{4}}

```out
'lisa'
```{{5}}

`@script`
각자 고유한 메서드를 가진 파이썬 객체입니다. 예를 들어 문자열을 나타내는 변수 sister를 보겠습니다.

sister에 입력 없이 capitalize 메서드를 호출할 수 있습니다. 그러면 첫 글자가 대문자로 바뀐 문자열을 반환합니다.

또 문자열의 일부를 다른 부분으로 바꾸고 싶다면 어떨까요? 문제 없습니다. sister에 replace 메서드를 적절한 두 입력과 함께 호출하면 됩니다.

출력에서는 "z"가 "sa"로 바뀌었습니다.

---

## 메서드

```yaml
type: FullSlide
key: 346697c688
code_zoom: 80
```

`@part1`
- 모든 것은 객체입니다{{1}}

- 객체에는 타입에 따른 메서드가 있습니다{{2}}

```py
sister.replace("z", "sa")
```{{3}}

```out
'lisa'
```{{3}}

```py
fam.replace("mom", "mommy")
```{{4}}

```out
AttributeError: 'list' object has no attribute 'replace'
```{{4}}

`@script`
정확히 말씀드리면, 파이썬에서는 모든 것이 객체이고, 각 객체에는 고유한 메서드가 연결되어 있습니다. 객체의 타입, 즉 list, string, float 등 무엇이냐에 따라 사용 가능한 메서드는 달라집니다. sister처럼 문자열 객체에는 replace 메서드가 있지만, fam처럼 리스트에는 이 메서드가 없습니다. 이 오류에서 볼 수 있듯이요.

---

## 메서드

```yaml
type: FullSlide
key: c0100c8d69
disable_transition: true
code_zoom: 80
```

`@part1`
```py
sister.index("z")
```{{1}}

```out
2
```{{1}}

```py
fam.index("mom")
```{{1}}

```out
4
```{{1}}

`@script`
서로 다른 타입의 객체가 이름이 같은 메서드를 가질 수도 있습니다. 예를 들어 index 메서드는 문자열과 리스트 모두에서 사용할 수 있습니다. 문자열에 호출하면 문자열 안의 글자 위치를, 리스트에 호출하면 리스트 안의 요소 위치를 돌려줍니다. 즉, 객체의 타입에 따라 메서드의 동작이 달라집니다.

메서드에 대한 연습 문제로 넘어가기 전에,

---

## 메서드 (2)

```yaml
type: FullSlide
key: f03ac21e34
code_zoom: 75
```

`@part1`
```py
fam
```{{1}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```{{1}}

```py
fam.append("me")
```{{2}}
```py
fam
```{{3}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me']
```{{3}}

```py
fam.append(1.79)
fam
```{{4}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me', 1.79]
```{{4}}

`@script`
한 가지를 더 알려 드리겠습니다. 어떤 메서드는 자신이 호출된 객체를 바꾸기도 합니다. fam 리스트로 돌아가서 append 메서드를 호출해 보겠습니다. 입력으로는 리스트에 추가할 문자열을 넘깁니다.

파이썬은 별도의 출력을 만들지 않지만, fam 리스트를 다시 보면 문자열 "me"가 추가된 것을 알 수 있습니다.

이번에는 제 키를 리스트에 추가해 보겠습니다.

다시 fam 리스트가 확장되었습니다.

이건 꽤 멋집니다. 데이터 구조를 바로바로 갱신하는 간결한 코드를 쓸 수 있으니까요. 하지만 위험할 수도 있습니다. 어떤 메서드는 객체를 바꾸지 않지만, 어떤 메서드는 바꾸므로 주의하셔야 합니다.

---

## 요약

```yaml
type: FullSlide
key: eecd826650
code_zoom: 80
```

`@part1`
함수{{1}}

```py
type(fam)
```{{2}}

```out
list
```{{2}}

메서드: 객체에 호출하는 함수{{3}}

```py
fam.index("dad")
```{{4}}

```out
6
```{{4}}

`@script`
여기서 잠깐 정리하겠습니다. type, max, round처럼 이렇게 호출하는 파이썬 함수가 있습니다. 그리고 메서드는 파이썬 객체에 특화된 함수입니다. 다루는 객체의 타입에 따라 사용할 수 있는 메서드가 달라지고 동작도 달라집니다. 점 표기법으로 이렇게 객체에 대해 메서드를 호출할 수 있습니다.

파이썬 객체, 메서드, 그리고 파이썬 내부 동작에 대해 더 이야기할 것이 많지만,

---

## 연습해 봅시다!

```yaml
type: FinalSlide
key: cefb86a284
```

`@script`
지금은 여기까지로 하겠습니다. 이제 연습을 통해 메서드를 여러분의 늘어나는 스킬셋에 추가해 보세요!
