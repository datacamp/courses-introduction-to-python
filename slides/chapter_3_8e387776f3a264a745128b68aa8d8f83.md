---
title: Insert title here
key: 8e387776f3a264a745128b68aa8d8f83
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/vi-VN/362723ca-12e5-4371-8bc3-454c554b4f8e.mp3
---

## Các phương thức

```yaml
type: TitleSlide
key: c536df1034
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
Các hàm có sẵn chỉ
---

## Các hàm có sẵn

```yaml
type: FullSlide
key: 45877294bd
```

`@part1`
- Giá trị lớn nhất của list: max(){{1}}

- Độ dài của list hoặc string: len(){{2}}

- Lấy index trong list: ?{{3}}

- Đảo ngược list: ?{{4}}

`@script`
là một phần trong thế giới Python. Bạn đã biết một số hàm như max để lấy giá trị lớn nhất của một list, len để lấy độ dài của một list hoặc một string, v.v. Nhưng còn những thao tác cơ bản khác thì sao, như lấy index của một phần tử cụ thể trong list, hoặc đảo ngược một list? Dù bạn tìm rất lâu trong các hàm có sẵn, nhưng sẽ không thấy hàm nào thực hiện những thao tác này.
---

## Trở lại với những điều cơ bản

```yaml
type: TwoColumns
key: a3e45f6524
code_zoom: 75
```

`@part1`
&nbsp;

```py
sister = "liz"
```

```py
height = 1.73
```{{1}}


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```{{2}}

`@part2`
![ch_3_2_slides.020.png](https://assets.datacamp.com/img/translations/vi-VN/production/repositories/288/datasets/c7a9757fa49f8396eb025ef221823441d6e66ced/ch_3_2_slides.020.png = 85){{3}}

`@script`
Trong các bài tập trước, chúng ta đã tạo khá nhiều biến. Bên cạnh các kiểu dữ liệu Python khác, chúng ta đã tạo string, float và list, như ta thấy ở đây. Mỗi giá trị hoặc cấu trúc dữ liệu này đều là các “đối tượng” trong Python. String này là một đối tượng, float này là một đối tượng, và list này cũng vậy, cũng là một đối tượng. Những đối tượng này có một kiểu dữ liệu cụ thể, mà bạn đã biết:
---

## Trở lại với những điều cơ bản

```yaml
type: TwoColumns
key: 6a5eddd6ea
disable_transition: true
code_zoom: 75
```

`@part1`
&nbsp;

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- Phương thức: Các hàm thuộc về các đối tượng{{1}}

`@part2`
![ch_3_2_slides.024.png](https://assets.datacamp.com/img/translations/vi-VN/production/repositories/288/datasets/6d444348823438f856363d02d093318f2ed457a3/ch_3_2_slides.024.png = 85)

`@script`
string, float và list, và tất nhiên chúng đại diện cho các giá trị bạn gán cho chúng, như "liz", 1,73 hay cả một list. Nhưng ngoài ra, các đối tượng trong Python còn đi kèm một loạt “phương thức”. Bạn có thể hiểu phương thức là những hàm “thuộc về” một đối tượng Python. Một đối tượng Python kiểu string có các phương thức
---

## Trở lại với những điều cơ bản

```yaml
type: TwoColumns
key: ff540e522c
disable_transition: true
code_zoom: 75
```

`@part1`
&nbsp;

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- Phương thức: Các hàm thuộc về các đối tượng

`@part2`
![ch_3_2_slides.028.png](https://assets.datacamp.com/img/translations/vi-VN/production/repositories/288/datasets/80891dbbb1a9b4f759540c5d601cbfb661a894d9/ch_3_2_slides.028.png = 85)

`@script`
như capitalize và replace; các đối tượng kiểu float và list cũng có những phương thức riêng tùy thuộc kiểu dữ liệu của chúng.

Lý thuyết đến đây thôi; chúng ta cùng thử thực hành một phương thức nào!
---

## Các phương thức với list

```yaml
type: FullSlide
key: 431cae8707
code_zoom: 85
```

`@part1`
```py
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam.index("mom") # "Call method index() on fam"
```{{1}}

```out
4
```{{2}}

```py
fam.count(1.73)
```{{3}}

```out
1
```{{4}}

`@script`
Giả sử ta muốn lấy index của chuỗi "mom" trong list fam. fam là một đối tượng Python kiểu list và có phương thức tên là index. Để gọi phương thức này, ta dùng kí hiệu dấu chấm như thế này. Đầu vào duy nhất là chuỗi "mom", tức phần tử bạn muốn lấy index. 

Python ra kết quả là 4, đúng là index của chuỗi "mom". Ở đây ta đã gọi phương thức index “trên” list fam và kết quả là 4. Tương tự, ta có thể dùng phương thức count trên list fam để đếm số lần 1,73 xuất hiện trong list.

Python trả kết quả là 1, kết quả hợp lý vì chỉ có liz cao 1,73 mét.

Nhưng không chỉ list mới có các phương thức đi kèm. Cả float, integer, boolean và string
---

## Các phương thức với chuỗi (string)

```yaml
type: FullSlide
key: 73c6a6ff3a
code_zoom: 80
```

`@part1`
```py
sister
```{{1}}

```out
'liz'
```{{1}}

```py
sister.capitalize()
```{{2}}

```out
'Liz'
```{{3}}

```py
sister.replace("z", "sa")
```{{4}}

```out
'lisa'
```{{5}}

`@script`
đều là các đối tượng trong Python và đều có những phương thức riêng. Lấy biến sister làm ví dụ, biến này đại diện cho một string.

Ta có thể gọi phương thức capitalize trên sister, không cần đầu vào. Kết quả trả về là một string mới với chữ cái đầu được viết hoa.

Hoặc nếu ta muốn thay thế một phần của string bằng phần khác? Rất dễ dàng. Chỉ cần gọi phương thức replace trên sister, với hai đầu vào phù hợp.

Kết quả, "z" đã được thay bằng "sa".
---

## Các phương thức

```yaml
type: FullSlide
key: 346697c688
code_zoom: 80
```

`@part1`
- Mọi thứ đều là đối tượng{{1}}

- Mỗi đối tượng có các phương thức liên quan, tùy thuộc vào kiểu dữ liệu{{2}}

```py
sister.replace("z", "sa")
```{{3}}

```out
'lisa'
```{{3}}

```py
fam.replace("mom", "mommy")
```{{4}}

```out
AttributeError: 'list' object has no attribute 'replace'
```{{4}}

`@script`
Nói rõ hơn: trong Python, mọi thứ đều là đối tượng, và mỗi đối tượng có những phương thức riêng đi kèm. Tùy vào từng kiểu dữ liệu của đối tượng — list, string, float, v.v. mà các phương thức có sẵn sẽ khác nhau. Một đối tượng string như sister có phương thức replace, nhưng một list như fam thì không. Điều này thể hiện thông qua lỗi bạn thấy ở đây.
---

## Các phương thức

```yaml
type: FullSlide
key: c0100c8d69
disable_transition: true
code_zoom: 80
```

`@part1`
```py
sister.index("z")
```{{1}}

```out
2
```{{1}}

```py
fam.index("mom")
```{{1}}

```out
4
```{{1}}

`@script`
Các đối tượng khác kiểu dữ liệu có thể có những phương thức trùng tên: ví dụ như phương thức index. Nó được dùng cho cả string và list. Nếu ta dùng nó trên string, ta sẽ có kết quả là vị trí của ký tự trong chuỗi; nếu ta dùng nó trên list, ta sẽ có kết quả là vị trí của phần tử trong list. Nghĩa là tùy thuộc vào kiểu dữ liệu của đối tượng, các phương thức sẽ cho ra kết quả khác nhau.
Trước khi làm các bài tập về phương thức,
---

## Các phương thức (2)

```yaml
type: FullSlide
key: f03ac21e34
code_zoom: 75
```

`@part1`
```py
fam
```{{1}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```{{1}}

```py
fam.append("me")
```{{2}}
```py
fam
```{{3}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me']
```{{3}}

```py
fam.append(1.79)
fam
```{{4}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me', 1.79]
```{{4}}

`@script`
còn một điều mình muốn chia sẻ. Một số phương thức có thể làm thay đổi chính đối tượng mà chúng được gọi trên đó. Hãy quay lại list fam và gọi phương thức append trên nó. Đầu vào là một string mà bạn muốn thêm vào list. 

Python không cho ra kết quả, nhưng nếu kiểm tra lại list fam, ta sẽ thấy nó đã được bổ sung string "me".

Làm thêm một lần nữa nhé, lần này thêm chiều cao của mình vào list.
Một lần nữa, list fam đã được mở rộng.

Điều này rất hay, vì bạn có thể viết code ngắn gọn để cập nhật cấu trúc dữ liệu ngay lập tức. Tuy nhiên cũng khá nguy hiểm. Một số phương thức không làm thay đổi đối tượng, một số khác lại làm thay đổi, nên hãy chú ý.
---

## Tóm tắt

```yaml
type: FullSlide
key: eecd826650
code_zoom: 80
```

`@part1`
Các hàm{{1}}

```py
type(fam)
```{{2}}

```out
list
```{{2}}

Các phương thức: Gọi hàm trên các đối tượng{{3}}

```py
fam.index("dad")
```{{4}}

```out
6
```{{4}}

`@script`
Giờ hãy dừng lại một chút để tổng kết. Chúng ta có các hàm Python như type, max và round, ta gọi chúng như thế này. Ta cũng có các phương thức, là các hàm dành riêng cho từng đối tượng trong Python. Tùy kiểu dữ liệu của đối tượng, ta sẽ dùng các phương thức khác nhau và chúng sẽ hoạt động khác nhau. Ta gọi phương thức trên đối tượng bằng ký hiệu dấu chấm, như ví dụ này.

Còn rất nhiều điều để nói về đối tượng Python, phương thức và cách Python vận hành bên trong,
---

## Cùng thực hành nào

```yaml
type: FinalSlide
key: cefb86a284
```

`@script`
nhưng bây giờ, hãy bám sát những gì mình vừa chia sẻ. Giờ là lúc làm bài tập và bổ sung phương thức vào bộ kỹ năng Python ngày càng phong phú của bạn!
