---
title: Insert title here
key: 8e387776f3a264a745128b68aa8d8f83
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/ko-KR/aa449305-132d-41cc-aea1-3010c7d751ac-4483f0858c0e4f8a543d66e685abf4ca-2.mp3
---

## 메서드

```yaml
type: TitleSlide
key: c536df1034
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
내장된 함수는
---

## 내장된 함수

```yaml
type: FullSlide
key: 45877294bd
```

`@part1`
- 리스트의 최댓값: max(){{1}}

- 리스트 또는 문자열의 길이: len(){{2}}

- 리스트에서 인덱스 얻기: ?{{3}}

- 리스트 뒤집기: ?{{4}}

`@script`
Python의 일부분일 뿐입니다. 여러분은 이미 리스트의 최댓값을 구하는 max, 리스트나 문자열의 길이를 구하는 len과 같은 함수에 대해 알고 있죠. 하지만 리스트에서 특정 요소의 인덱스를 얻거나, 리스트를 뒤집는 등 다른 기본적인 것들은 어떻게 할까요? 내장 함수를 아무리 열심히 찾아봐도 이런 기능을 하는 함수를 찾을 수 없을 겁니다.

---

## 기초 복습

```yaml
type: TwoColumns
key: a3e45f6524
code_zoom: 75
```

`@part1`
 

```py
sister = "liz"
```

```py
height = 1.73
```{{1}}


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```{{2}}

`@part2`
![ch_3_2_slides.020.png](https://assets.datacamp.com/img/translations/ko-KR/production/repositories/288/datasets/c7a9757fa49f8396eb025ef221823441d6e66ced/ch_3_2_slides.020.png = 85) {{3}}

`@script`
이전 연습 문제에서 여러분은 이미 여러 개의 변수를 만들었습니다. Python의 여러 타입 중에서 여러분은 문자열, 실수형, 리스트를 만들어 보았습니다. 화면에 보이는 것처럼요. 각각의 값이나 데이터 구조는 Python 객체라고 불립니다. 이 문자열도 객체이고, 이 실수형도 객체이며, 예상하셨듯이 이 리스트 역시 객체입니다. 이 객체들은 특정한 타입을 가지는데, 이미 알고 계시는 것처럼,

---

## 기초 복습

```yaml
type: TwoColumns
key: 6a5eddd6ea
disable_transition: true
code_zoom: 75
```

`@part1`
 

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- 메서드: 객체에 속한 함수{{1}}

`@part2`
![ch_3_2_slides.024.png](https://assets.datacamp.com/img/translations/ko-KR/q2/production/repositories/288/datasets/6d444348823438f856363d02d093318f2ed457a3/ch_3_2_slides.024.jpg = 85)

`@script`
문자열과 실수형, 리스트입니다. 그리고 각각 liz나 1.73처럼 여러분이 할당한 값, 혹은 전체 리스트를 나타냅니다. 여기에 더해서, Python 객체에는 다수의 '메서드'도 포함됩니다. 메서드는 Python 객체에 '속해 있는' 함수라고 생각하시면 됩니다. 문자열 타입의 Python 객체에는 메서드가 있는데,

---

## 기초 복습

```yaml
type: TwoColumns
key: ff540e522c
disable_transition: true
code_zoom: 75
```

`@part1`
 

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- 메서드: 객체에 속한 함수

`@part2`
![ch_3_2_slides.028.png](https://assets.datacamp.com/img/translations/ko-KR/q2/production/repositories/288/datasets/80891dbbb1a9b4f759540c5d601cbfb661a894d9/ch_3_2_slides.028.jpg = 85)

`@script`
capitalize나 replace 같은 것들입니다. 실수와 리스트 타입의 객체도 타입에 따라 특정한 메서드를 가집니다.

이론은 이 정도면 충분합니다. 이제 메서드를 사용해볼까요?

---

## 리스트 메서드

```yaml
type: FullSlide
key: 431cae8707
code_zoom: 85
```

`@part1`
```py
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam.index("mom") # "Call method index() on fam"
```{{1}}

```out
4
```{{2}}

```py
fam.count(1.73)
```{{3}}

```out
1
```{{4}}

`@script`
fam 리스트에서 문자열 'mom'의 인덱스를 얻고 싶다고 가정해봅시다. fam은 Python 객체로, 리스트 타입이며, index라는 메서드를 가지고 있습니다. 메서드를 호출하기 위해 점 표기법을 사용합니다. 이렇게요. 유일한 입력값은 'mom'이라는 문자열로, 여러분이 인덱스를 얻고자 하는 요소입니다.

Python은 4를 반환합니다. 4는 문자열 'mom'의 실제 인덱스죠. 제가 fam 리스트에서 index 메서드를 호출했고, 출력값은 4였습니다. 마찬가지로, fam 리스트에서 count 메서드를 사용하여 1.73이 리스트에 몇 번 나타나는지 셀 수 있습니다.

Python은 1을 반환합니다. 타당한 결과죠. 키가 1.73미터인 건 liz뿐이기 때문입니다.

메서드가 있는 Python 객체가 리스트뿐인 것은 아닙니다. 실수형과 정수형, 불리언, 문자열 또한

---

## 문자열 메서드

```yaml
type: FullSlide
key: 73c6a6ff3a
code_zoom: 80
```

`@part1`
```py
sister
```{{1}}

```out
'liz'
```{{1}}

```py
sister.capitalize()
```{{2}}

```out
'Liz'
```{{3}}

```py
sister.replace("z", "sa")
```{{4}}

```out
'lisa'
```{{5}}

`@script`
특정 메서드가 있는 Python 객체입니다. 예를 들어, 문자열을 나타내는 변수 sister를 살펴보겠습니다.

sister에서 capitalize 메서드를 아무 입력값 없이 호출할 수 있습니다. 그럼 첫 글자가 대문자인 문자열이 반환되죠.

만약 문자열의 일부를 다른 부분으로 바꾸고 싶다면 어떻게 해야 할까요? 아주 간단합니다. sister에서 replace 메서드를 호출하고, 적절한 입력값 두 개를 넣으면 됩니다.

결괏값에서 "z"는 "sa"로 대체됩니다.

---

## 메서드

```yaml
type: FullSlide
key: 346697c688
code_zoom: 80
```

`@part1`
- 모든 것 = 객체{{1}}

- 타입에 따라 객체에 메서드가 연결됨{{2}}

```py
sister.replace("z", "sa")
```{{3}}

```out
'lisa'
```{{3}}

```py
fam.replace("mom", "mommy")
```{{4}}

```out
AttributeError: 'list' object has no attribute 'replace'
```{{4}}

`@script`
분명하게 말씀드리자면, Python에서는 모든 것이 객체이고, 각 객체에 특정 메서드가 연결되어 있습니다. 리스트, 문자열, 실수형 등 객체의 타입에 따라 사용 가능한 메서드가 다릅니다. sister와 같은 문자열 객체에는 replace 메서드가 있지만, fam과 같은 리스트에는 이 메서드가 없습니다. 이 오류를 보면 알 수 있죠.

---

## 메서드

```yaml
type: FullSlide
key: c0100c8d69
disable_transition: true
code_zoom: 80
```

`@part1`
```py
sister.index("z")
```{{1}}

```out
2
```{{1}}

```py
fam.index("mom")
```{{1}}

```out
4
```{{1}}

`@script`
서로 다른 타입의 객체가 동일한 이름의 메서드를 가질 수도 있습니다. index 메서드를 생각해보세요. 문자열과 리스트 모두에서 사용할 수 있는 메서드죠. 문자열에서 호출하면 문자열 속 문자의 인덱스를 얻을 수 있고, 리스트에서 호출하면 리스트 요소의 인덱스를 얻을 수 있습니다. 즉, 객체의 유형에 따라 메서드가 다르게 동작한다는 거죠.

메서드에 대한 연습 문제를 몇 개 풀어보기 전에,

---

## 메서드(2)

```yaml
type: FullSlide
key: f03ac21e34
code_zoom: 75
```

`@part1`
```py
fam
```{{1}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```{{1}}

```py
fam.append("me")
```{{2}}
```py
fam
```{{3}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me']
```{{3}}

```py
fam.append(1.79)
fam
```{{4}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me', 1.79]
```{{4}}

`@script`
한 가지만 더 말씀드리겠습니다. 일부 메서드는 호출된 객체를 변경할 수 있습니다. fam 리스트를 다시 가져와서, append 메서드를 호출해 보겠습니다. 리스트에 추가하고 싶은 문자열을 입력값으로 전달합니다.

Python은 결과를 반환하지 않지만, fam 리스트를 다시 확인해 보면 'me'라는 문자열이 추가된 것을 볼 수 있습니다.

다시 한 번 해 볼까요? 이번에는 여러분의 키를 리스트에 추가해 보죠.

다시 한 번, fam 목록이 확장되었습니다.

꽤나 멋집니다. 간결한 코드를 작성해 데이터 구조를 즉석에서 바로 업데이트할 수 있거든요. 하지만 동시에 상당히 위험할 수도 있습니다. 일부 메서드 호출은 호출된 객체를 변경하지 않지만, 객체를 변경하는 메서드도 있으므로 주의해야 합니다.

---

## 요약

```yaml
type: FullSlide
key: eecd826650
code_zoom: 80
```

`@part1`
함수{{1}}

```py
type(fam)
```{{2}}

```out
list
```{{2}}

메서드: 객체에서 함수 호출{{3}}

```py
fam.index("dad")
```{{4}}

```out
6
```{{4}}

`@script`
이쯤에서 잠시 멈춰서 내용을 정리해보겠습니다. Python에는 type, max, round와 같은 다양한 함수가 있으며, 이렇게 호출할 수 있습니다. 또한, Python 객체에 특화된 함수인 메서드도 있습니다. 다루는 Python 객체의 유형에 따라 다른 메서드를 사용할 수 있으며, 각 메서드는 서로 다르게 동작합니다. 점 표기법을 사용해 객체에서 메서드를 호출할 수 있습니다. 이런 식으로요.

Python 객체와 메서드를 비롯하여, Python이 내부적으로 어떻게 동작하는지에 대해 알려드릴 것이 많지만,

---

## 연습해봅시다!

```yaml
type: FinalSlide
key: cefb86a284
```

`@script`
우선은 여기서 이야기한 내용에만 집중해보겠습니다. 이제 연습 문제를 풀고, 메서드를 잘 익혀서 여러분의 기술을 확장해보세요!
