---
title: Insert title here
key: 8e387776f3a264a745128b68aa8d8f83
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/pt-BR/5dad417a-cf56-46e2-9d7d-73566fda6c7b.mp3
---

## Métodos

```yaml
type: TitleSlide
key: c536df1034
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
As funções integradas são apenas

---

## Funções integradas

```yaml
type: FullSlide
key: 45877294bd
```

`@part1`
- Máximo da lista: max(){{1}}

- Comprimento da lista ou string: len(){{2}}

- Obter um índice da lista: ?{{3}}

- Inverter uma lista: ?{{4}}

`@script`
uma parte da história do Python. Você já conhece funções como max, para pegar o maior valor de uma lista, len, para ver o tamanho de uma lista ou string, e assim por diante. Mas e quanto a outras coisas básicas, como pegar o índice de um elemento específico na lista ou inverter uma lista? Você pode ficar um tempão procurando funções integradas que façam isso, mas não vai achar.


---

## De volta ao básico

```yaml
type: TwoColumns
key: a3e45f6524
code_zoom: 75
```

`@part1`
&nbsp;

```py
sister = "liz"
```

```py
height = 1.73
```{{1}}


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```{{2}}

`@part2`
![ch_3_2_slides.020.png](https://assets.datacamp.com/img/translations/pt-BR/production/repositories/288/datasets/c7a9757fa49f8396eb025ef221823441d6e66ced/ch_3_2_slides.020.png = 85){{3}}

`@script`
Nos exercícios anteriores, você já criou diversas variáveis. Entre outros tipos Python, você criou strings, floats e listas, como estas aqui. Cada um desses valores ou estruturas de dados são chamados de objetos Python. Esta string é um objeto, este float é um objeto, mas esta lista também é, você acertou, um objeto. Estes objetos têm um tipo específico, que você já conhece,


---

## De volta ao básico

```yaml
type: TwoColumns
key: 6a5eddd6ea
disable_transition: true
code_zoom: 75
```

`@part1`
&nbsp;

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- Métodos: funções que pertencem a objetos{{1}}

`@part2`
![ch_3_2_slides.024.png](https://assets.datacamp.com/img/translations/pt-BR/production/repositories/288/datasets/6d444348823438f856363d02d093318f2ed457a3/ch_3_2_slides.024.png = 85)

`@script`
string, float e list, e é claro que eles representam os valores que você deu a eles, como “liz”, um ponto setenta e três e uma lista inteira. Mas, além disso, os objetos Python também vêm com uma série dos chamados “métodos”. Pense nos métodos como funções que “pertencem” a objetos Python. Um objeto Python do tipo string tem métodos


---

## De volta ao básico

```yaml
type: TwoColumns
key: ff540e522c
disable_transition: true
code_zoom: 75
```

`@part1`
&nbsp;

```py
sister = "liz"
```

```py
height = 1.73
```


```py
fam = ["liz", 1.73, "emma", 1.68,
       "mom", 1.71, "dad", 1.89]
```

- Métodos: funções que pertencem a objetos

`@part2`
![ch_3_2_slides.028.png](https://assets.datacamp.com/img/translations/pt-BR/production/repositories/288/datasets/80891dbbb1a9b4f759540c5d601cbfb661a894d9/ch_3_2_slides.028.png = 85)

`@script`
como “capitalize” e “replace”, mas objetos do tipo float e list também têm métodos específicos, dependendo do tipo.

Chega de teoria por enquanto; vamos tentar usar um método!


---

## Métodos para listas

```yaml
type: FullSlide
key: 431cae8707
code_zoom: 85
```

`@part1`
```py
fam
```

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```

```py
fam.index("mom") # "Call method index() on fam"
```{{1}}

```out
4
```{{2}}

```py
fam.count(1.73)
```{{3}}

```out
1
```{{4}}

`@script`
Suponha que você queira pegar o índice da string “mom” na lista fam. fam é um objeto Python do tipo list e tem um método chamado index. Para chamar o método, você usa a notação de ponto, assim. A única entrada é a string “mom”, o elemento do qual você quer saber o índice.

Python retorna quatro, que realmente é o índice da string “mom”. Chamei o método index com a lista fam, e o resultado foi quatro. Da mesma forma, posso usar o método count com a lista fam para contar quantas vezes um ponto setenta e três aparece na lista.

O Python mostra um, o que faz sentido, porque só a Liz tem um metro e setenta e três de altura.

Mas as listas não são os únicos objetos Python que têm métodos associados. Os floats, inteiros, booleanos e strings


---

## Métodos para strings

```yaml
type: FullSlide
key: 73c6a6ff3a
code_zoom: 80
```

`@part1`
```py
sister
```{{1}}

```out
'liz'
```{{1}}

```py
sister.capitalize()
```{{2}}

```out
'Liz'
```{{3}}

```py
sister.replace("z", "sa")
```{{4}}

```out
'lisa'
```{{5}}

`@script`
são objetos Python que têm métodos específicos associados a eles. Veja a variável sister, por exemplo, que representa uma string.

Você pode chamar o método “capitalize” com sister, sem nenhuma entrada. Ele retorna uma string em que a primeira letra agora está em maiúscula.

E se você quiser trocar algumas partes da string por outras? Sem problema. É só chamar o método “replace” com sister, usando duas entradas válidas.

Na saída, “z” é substituído por “sa”.


---

## Métodos

```yaml
type: FullSlide
key: 346697c688
code_zoom: 80
```

`@part1`
- Tudo = objeto{{1}}

- Os objetos têm métodos associados, dependendo do tipo{{2}}

```py
sister.replace("z", "sa")
```{{3}}

```out
'lisa'
```{{3}}

```py
fam.replace("mom", "mommy")
```{{4}}

```out
AttributeError: 'list' object has no attribute 'replace'
```{{4}}

`@script`
Para ficar bem claro: em Python, tudo é um objeto, e cada objeto tem métodos específicos associados. Dependendo do tipo do objeto, list, string, float ou qualquer outro, os métodos disponíveis são diferentes. Um objeto string como sister tem um método “replace”, mas uma lista como fam não tem, como dá para ver neste erro.


---

## Métodos

```yaml
type: FullSlide
key: c0100c8d69
disable_transition: true
code_zoom: 80
```

`@part1`
```py
sister.index("z")
```{{1}}

```out
2
```{{1}}

```py
fam.index("mom")
```{{1}}

```out
4
```{{1}}

`@script`
Objetos de tipos diferentes podem ter métodos com o mesmo nome. Observe o método index. Está disponível tanto para strings quanto para listas. Se você chamá-lo com uma string, vai obter o índice das letras na string; se chamá-lo com uma lista, vai obter o índice do elemento na lista. Isso quer dizer que, dependendo do tipo de objeto, os métodos funcionam de maneira diferente.

Antes de deixar você fazer alguns exercícios sobre métodos,


---

## Métodos (2)

```yaml
type: FullSlide
key: f03ac21e34
code_zoom: 75
```

`@part1`
```py
fam
```{{1}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89]
```{{1}}

```py
fam.append("me")
```{{2}}
```py
fam
```{{3}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me']
```{{3}}

```py
fam.append(1.79)
fam
```{{4}}

```out
['liz', 1.73, 'emma', 1.68, 'mom', 1.71, 'dad', 1.89, 'me', 1.79]
```{{4}}

`@script`
tem mais uma coisa que quero dizer. Alguns métodos podem modificar os objetos com os quais são chamados. Vamos pegar a lista fam de novo e chamar o método append com ela. Como entrada, passamos uma string que queremos incluir na lista.

O Python não gera uma saída, mas, se verificarmos a lista fam de novo, vemos que ela aumentou e agora contém a string “me”.

Vamos fazer isso de novo, desta vez para incluir minha altura na lista.

Mais uma vez, a lista fam aumentou.

Isso é muito legal, porque você pode escrever um código bem curto para atualizar estruturas de dados na hora, mas também pode ser bem perigoso. Algumas chamadas de métodos não alteram o objeto com o qual são chamados, enquanto outras sim, então tome cuidado.


---

## Resumo

```yaml
type: FullSlide
key: eecd826650
code_zoom: 80
```

`@part1`
Funções{{1}}

```py
type(fam)
```{{2}}

```out
list
```{{2}}

Métodos: chamam funções com objetos{{3}}

```py
fam.index("dad")
```{{4}}

```out
6
```{{4}}

`@script`
Vamos dar um passo atrás e resumir isso. Existem funções Python, como type, max e round, que podemos chamar assim. Também existem métodos, que são funções específicas de objetos Python. Dependendo do tipo de objeto Python com que estamos lidando, podemos usar métodos diferentes, e eles se comportam de forma diferente. Podemos chamar métodos com os objetos usando a notação de ponto, assim, por exemplo.

Daria para falar muito mais sobre objetos e métodos do Python e como ele funciona por dentro,


---

## Vamos praticar!

```yaml
type: FinalSlide
key: cefb86a284
```

`@script`
mas, por enquanto, vamos nos ater ao que falei aqui. É hora de fazer alguns exercícios e incluir métodos no seu conjunto de habilidades que não para de crescer!
