---
title: Insert title here
key: 1204d914b0e53100529827e07441ee6c
video_link:
  mp3: >-
    https://videos.datacamp.com/mp3/translations/course_735/ro-RO/00c8e6a2-5915-4d96-989e-19a131210a20-317f1ffbb60bb6211b0c138743e81ab2.mp3
---

## Funcții

```yaml
type: TitleSlide
key: 6d7066bcd2
```

`@lower_third`
name: Hugo Bowne-Anderson
title: Data Scientist at DataCamp

`@script`
În acest videoclip, îți voi prezenta funcțiile. Odată ce le descoperi, nu te mai poți opri din a le folosi. Eu cel puțin nu mă pot.

---

## Funcții

```yaml
type: FullSlide
key: 5f508018d7
```

`@part1`
- Nimic nou!{{1}}

- `type()`{{2}}

- Cod reutilizabil{{3}}

- Rezolvă o sarcină specifică{{4}}

- Apelați funcția în loc să scrieți cod de la zero{{5}}

`@script`
De fapt, funcțiile nu sunt ceva complet nou pentru tine. Le-ai mai folosit. De exemplu, type este o funcție care returnează tipul unei valori. Dar ce este, mai exact, o funcție? Pe scurt, o funcție este o bucată de cod reutilizabil, creată pentru a rezolva o anumită problemă. Poți apela funcții în loc să scrii codul tu însuți. Un exemplu ne va lămuri.

---

## Exemplu

```yaml
type: FullSlide
key: c2afbb6435
code_zoom: 75
```

`@part1`
```py
fam = [1.73, 1.68, 1.71, 1.89]
fam
```

```out
[1.73, 1.68, 1.71, 1.89]
```

```py
max(fam)
```{{1}}

```out
1.89
```{{1}}

![ch_3_1_slides.012.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/efef98eb50aba2b36df52166f7c4b18fd89c62e1/ch_3_1_slides.012.png){{2}}

`@script`
Să presupunem că ai lista cu înălțimile familiei tale, fam.

Vrei să găsești valoarea maximă din această listă. În loc să scrii tu un cod Python care parcurge lista și găsește cea mai mare valoare, poți folosi funcția max din Python. Aceasta este una dintre funcțiile integrate ale limbajului, la fel ca type. Îi pasăm pur și simplu lista fam, între paranteze.

Rezultatul este cel așteptat: 1,89, cel mai mare număr din listă.

max a funcționat ca o cutie neagră.

---

## Exemplu

```yaml
type: FullSlide
key: 46af509641
disable_transition: true
code_zoom: 75
```

`@part1`
```py
fam = [1.73, 1.68, 1.71, 1.89]
fam
```

```out
[1.73, 1.68, 1.71, 1.89]
```

```py
max(fam)
```

```out
1.89
```

![ch_3_1_slides.013.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/65f70092ec124c8f29a082f9409e473496806aaa/ch_3_1_slides.013.png)

`@script`
I-ai dat o listă, apoi implementarea funcției max, pe care nu o cunoști, și-a făcut treaba,

---

## Exemplu

```yaml
type: FullSlide
key: c575524d98
disable_transition: true
code_zoom: 75
```

`@part1`
```py
fam = [1.73, 1.68, 1.71, 1.89]
fam
```

```out
[1.73, 1.68, 1.71, 1.89]
```

```py
max(fam)
```

```out
1.89
```

![ch_3_1_slides.014.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/404545609ab865031039dcfd81ea2d2962126f72/ch_3_1_slides.014.png)

`@script`
și a produs un rezultat. Nu contează cum face asta max în interior. Important e că funcționează cum trebuie, fără să fi scris tu vreun cod în plus.

---

## Exemplu

```yaml
type: FullSlide
key: bed6186ee9
disable_transition: true
code_zoom: 75
```

`@part1`
```py
fam = [1.73, 1.68, 1.71, 1.89]
fam
```

```out
[1.73, 1.68, 1.71, 1.89]
```

```py
max(fam)
```

```out
1.89
```

```py
tallest = max(fam)
tallest
```{{1}}

```out
1.89
```{{1}}

`@script`
Bineînțeles, poți să atribui rezultatul unui apel de funcție unei noi variabile, ca aici. Acum tallest este o variabilă ca oricare alta, pe care o poți folosi în continuare în calculele tale.

---

## round()

```yaml
type: FullSlide
key: b6626f6bff
code_zoom: 62
```

`@part1`
```py
round(1.68, 1)
```{{1}}

```out
1.7
```{{1}}

```py
round(1.68)
```{{2}}

```out
2
```{{2}}

```py
help(round) # Open up documentation
```{{3}}

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```{{3}}

`@script`
O altă funcție integrată este round. Primește două valori de intrare: mai întâi, numărul pe care vrei să îl rotunjești, și apoi precizia de rotunjire, adică numărul de zecimale pe care vrei să le păstrezi. Să zicem că vrei să rotunjești 1,68 la o zecimală. Prima valoare de intrare este 1,68, a doua este 1. Le separi printr-o virgulă.

Dar mai e ceva. Poți apela funcția round cu o singură valoare de intrare, ca în exemplul acesta. De data aceasta, Python observă că nu ai specificat a doua valoare și rotunjește automat numărul la cel mai apropiat număr întreg.

Ca să înțelegem de ce funcționează ambele variante, hai să deschidem documentația. Poți face asta cu o altă funcție, help, ca aici.

Observăm că round primește două valori de intrare.

---

## round()

```yaml
type: FullSlide
key: c8119a3588
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```



![ch_3_1_slides.026.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/27ffd63d62347e84e5471ee64a8652616e575616/ch_3_1_slides.026.png){{1}}

`@script`
În Python, aceste valori de intrare, numite și argumente, au nume: number și ndigits. Când apelezi funcția round

---

## round()

```yaml
type: FullSlide
key: 8aacabb9b1
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.027.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/0b07066836b79b6c2539ddda423da6ff6352ddf6/ch_3_1_slides.027.png)

`@script`
cu aceste două valori de intrare, Python le asociază argumentelor corespunzătoare.

---

## round()

```yaml
type: FullSlide
key: 0ae8191d5a
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.028.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/4c257fe9ca0994487db6141be7376a370a81d25f/ch_3_1_slides.028.png)

`@script`
number primește valoarea 1,68, iar

---

## round()

```yaml
type: FullSlide
key: 061bc680d8
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.029.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/26344efb7eb778da4d8c3350f79dadd82a8a6fd1/ch_3_1_slides.029.png)

`@script`
ndigits primește valoarea 1. Apoi,

---

## round()

```yaml
type: FullSlide
key: 7289eaeb61
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.030.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/a7d825885a0519ffec79b0763684cd8b16822d6e/ch_3_1_slides.030.png)

`@script`
funcția round efectuează calculele folosind number și ndigits ca și cum ar fi variabile Python obișnuite. Nu știm exact ce cod execută Python în interior. Important este că funcția produce un rezultat,

---

## round()

```yaml
type: FullSlide
key: b5ef829b0c
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.031.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/c4e016f38f0612354160324d2e2abe0ce922a4f3/ch_3_1_slides.031.png)

`@script`
și anume numărul 1,68 rotunjit la 1 zecimală.

---

## round()

```yaml
type: FullSlide
key: c02d6edac7
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```



![ch_3_1_slides.032.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/27ffd63d62347e84e5471ee64a8652616e575616/ch_3_1_slides.032.png)

`@script`
Dacă apelezi funcția round cu o singură valoare de intrare,

---

## round()

```yaml
type: FullSlide
key: 7c246e950e
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.033.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/29fb0a5fe3ca2ea269fc4c82815591b9bca55d5e/ch_3_1_slides.033.png)

`@script`
Python încearcă din nou

---

## round()

```yaml
type: FullSlide
key: 51e45534c3
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.034.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/4167c94aecf6b66c78efaf5f8ac9232187fb23df/ch_3_1_slides.034.png)

`@script`
să asocieze valorile de intrare

---

## round()

```yaml
type: FullSlide
key: e33598e422
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.035.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/1218fd4989e4f6dfd8471d5cf0f88da0189efc27/ch_3_1_slides.035.png)

`@script`
argumentelor. De data aceasta însă, nu există nicio valoare de intrare pentru argumentul ndigits. Din fericire,

---

## round()

```yaml
type: FullSlide
key: 767966a5a9
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.036.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/b8f1b94ac3acfdd400bbdf1fca652f772cee4ae6/ch_3_1_slides.036.png)

`@script`
mecanismul intern al funcției round știe cum să gestioneze această situație. Când ndigits nu este specificat, funcția rotunjește pur și simplu la cel mai apropiat număr întreg și

---

## round()

```yaml
type: FullSlide
key: 93b669c9cb
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

![ch_3_1_slides.037.png](https://assets.datacamp.com/img/translations/ro-RO/production/repositories/288/datasets/0a9ca09bc0b46f05f77483d00fb1eadadfc75033/ch_3_1_slides.037.png)

`@script`
returnează acel număr întreg. De aceea am obținut valoarea 2.

---

## round()

```yaml
type: FullSlide
key: eed1e60402
disable_transition: true
code_zoom: 63
```

`@part1`
```py
help(round)
```

```out
Help on built-in function round in module builtins:

round(number, ndigits=None)
    Round a number to a given precision in decimal digits.
    
    The return value is an integer if ndigits is omitted or None. 
    Otherwise the return value has the same type as the number. ndigits may be negative. 
```

- `round(number)`{{1}}
- `round(number, ndigits)`{{2}}

`@script`
Cu alte cuvinte, ndigits este un argument opțional. Asta înseamnă că poți apela round în ambele forme pe care le-ai văzut.

---

## Găsirea funcțiilor

```yaml
type: FullSlide
key: a9853a9d66
```

`@part1`
- Cum aflăm?{{1}}

- Sarcină standard -> probabil există o funcție!{{2}}

- Internetul este prietenul dumneavoastră{{3}}

`@script`
Până acum știi cum să folosești max și round, dar de unde ai ști că o funcție precum round există în Python? Ei bine, asta vei învăța cu timpul. Ori de câte ori ai de rezolvat o sarcină obișnuită în Python, poți fi aproape sigur că există deja o funcție care o face pentru tine. În acel caz, folosește-o fără ezitare. O căutare rapidă pe internet îți va găsi funcția de care ai nevoie, împreună cu un exemplu de utilizare. Și, bineînțeles, ai DataCamp, unde vei descoperi funcții utile și vei învăța cum să le folosești.

---

## Să exersăm!

```yaml
type: FinalSlide
key: dbac5490bd
```

`@script`
Treci direct la exercițiile interactive și ne vedem în curând!
